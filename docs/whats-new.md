## 2026-06-01T18:14:09.377981+00:00
- PR #113: Introduces a pure helper `stageRanInEntry(entry, legacyNumber, newName)` that detects `/ship` stage execution across all three journal format generations (singular `entry.stage`, legacy-numeric `stages_run`, and new-string `stages_run`). `gatherShipJournal` is updated to use this helper for both a widened `stage2Count` (verify-agent / Stage 2) and a new `simplifyStageCount` (simplify / Stage 3). `run-assessment.mjs` MAX-merges the new `simplifyStageCount` journal counter into the `simplifyCommandUses` projection, mirroring the v0.9.16 `/color` pattern. The journal lookback window is also widened from 14 to `insightsLookbackDays` (default 30 days) to align it with transcript-derived signals. The probe implementation status tracker is updated accordingly, and new plan and design spec docs are committed.
- PR #111: Two-line edit to the `docs-agent-nightly` GitHub Actions workflow: (1) migrated the GitHub App token step from the deprecated `app-id` input (backed by a secret) to `client-id` (backed by a repository variable `DOCS_AGENT_APP_CLIENT_ID`), per the upstream `actions/create-github-app-token@v3` deprecation; and (2) moved `JIRA_EMAIL` from `secrets.JIRA_EMAIL` to `vars.JIRA_EMAIL` because an Atlassian account email is a non-sensitive basic-auth username, not a credential. No logic or step order changed.
- PR #110: Introduces a per-command posture-vs-volume partition in the transcript scanner (`scripts/_usage-data.mjs`). Posture commands (`/color`, `/voice`, `/focus`, `/btw`, `/clear`, `/compact`, `/simplify`, `/rewind`, `/fewer-permission-prompts`) are now counted only when `classifySessionKind` returns `interactive_cli` or `unknown`, so observer and SDK-orchestrated sessions no longer inflate posture counters by echoing the primary session's command markup. Volume commands (`/loop`, `/schedule`, `/babysit`, `/go`, `/batch`) remain counted across all session kinds. A fail-loud `assertCommandPartition` boundary assertion runs at module load to catch partition drift (disjointness, missing classification, dead classification). The partition is codified in CLAUDE.md, a new design spec, an implementation plan, and the probe-implementation-status tracker.
- PR #108: Added three new telemetry-dated milestone detectors to the `/progression` timeline — `scheduled` (tip 48, triggers on first `/loop`, `/schedule`, or `/babysit` invocation), `remote` (tip 35, triggers on first `RemoteTrigger`, `PushNotification`, or `SendMessage` tool fire), and `verification` (tip 73, triggers on first `/go`). This closes the coverage gap filed as CCE-33: the timeline previously tracked 8 of 12 scored dimensions; it now covers 11. A supporting additive change to `scanTranscriptModes` in `_usage-data.mjs` exposes a per-session `commands: Set<string>` field consumed by the two transcripts-dependent detectors. Accompanying design spec and implementation plan docs were added, and the probe-implementation-status tracker was updated to reflect the new detectors.
- PR #107: Pure test-suite hardening following the PR #106 predicate-ranker review cycle. Two deferred Minor findings are addressed: an explicit regression test pins the internal `axisOrder()` mapping, and the `evaluatePredicate` equality path gains an assertion that string coercion (`x=5` with `x: "5"`) returns `true`. A leftover WIP stash entry (`stash@{0}`) is also dropped. No production code was modified.
- PR #106: PR #106 establishes a single canonical DSL predicate evaluator by extracting `scripts/predicate.mjs` (a byte-faithful port from `app/lib/assessment.ts`) and collapsing `app/lib/assessment.ts:evaluatePredicate` to a 2-line import/re-export. It also adds `scripts/rank-next-actions.mjs` (`rankNextActions(rubric, scoreMap, signalsSummary, limit=10)`) wired into `scripts/run-assessment.mjs`, so `assessment.json` now carries a pre-computed `rankedNextActions` top-10 list. The `/self-assessment` skill is trimmed to read that pre-computed list instead of hand-implementing the filter. New tests cover predicate evaluation, ranking logic, and an equivalence assertion that the TS re-export and the `.mjs` canonical implementation are reference-equal.
- PR #104: Adds a 12-line `satisfiedWhen` DSL grammar block to `.claude/skills/self-assessment/SKILL.md` documenting all 7 operator classes, plus a new design spec (282 lines) and implementation plan (1186 lines) for the predicate-ranker feature. The grammar block is a tactical documentation fix; the spec and plan capture the architectural decision and full implementation roadmap for surfacing a Node-side DSL evaluator so the skill never hand-rolls its own filter logic.
- PR #102: Fixed a one-character schema error in `.engineering-docs-agent/state.json`: the `dismissed_gap_flags` field was seeded as an empty array (`[]`) by the bootstrap PR, but the plugin's state schema requires it to be an empty object (`{}`), keyed by `{owner}/{name}#{pr}` strings. This single-line correction unblocks the docs-agent nightly pipeline.
- PR #101: Added a new Conventions entry to CLAUDE.md documenting the `/progression` timeline's dual milestone-source architecture: telemetry milestones (9 detectors, self-dated from session `start_time` across full history via `scripts/progression.mjs`) merged with config milestones (8 detectors, `firstSeenAt` stamped on first run and frozen in `app/data/progression-config.json` via `scripts/config-progression.mjs`). Recorded the known coverage gap—3 of 12 scored dimensions (`scheduled`, `remote`, `verification`) have no milestone detector, tracked as CCE-33. Corrected the file map by adding the previously missing `config-progression.mjs` entry and fixing a stale comment on `app/progression/page.tsx`.
- PR #100: PR #100 bootstraps the engineering-docs-agent plugin for the claude-code-self-assessment repository. It introduces the plugin configuration file (`.engineering-docs-agent/config.yml`, 46 lines) declaring the host as `framework: none` — a first-class config value added in CCE-64 — along with an example and live state file seeding initial plugin state, and a new GitHub Actions nightly workflow (`.github/workflows/docs-agent-nightly.yml`, 143 lines) that triggers automated documentation maintenance runs on a schedule.
### Gaps flagged
- theoju/claude-code-self-assessment#113: This PR introduces a new scoring helper (`stageRanInEntry`), widens journal-format coverage (fixing ~41% miss in stage detection), adds a new `simplifyStageCount` signal, and changes user-visible scoring output (`simplifyCommandUses`). It also widens the lookback window from 14 to `insightsLookbackDays`. These are meaningful behavioral changes to the scoring pipeline — the kind a senior engineer would expect to be accompanied by a written design spec and plan. The PR itself confirms this by shipping both `docs/superpowers/specs/2026-06-01-ship-journal-stage-credit-design.md` (325 lines) and `docs/superpowers/plans/2026-06-01-ship-journal-stage-credit.md` (888 lines).
- theoju/claude-code-self-assessment#110: This PR changes core scoring logic in _usage-data.mjs — introducing a posture-vs-volume command partition that alters how user-visible scores are computed and adds a fail-loud module-load assertion that affects production behavior. It introduces a new subsystem boundary (POSTURE_COMMANDS vs VOLUME_COMMANDS classification + assertCommandPartition enforcement). A senior engineer would expect a written spec/plan for a behavioral scoring change of this kind. Notably, the PR itself ships a design spec (2026-05-31-per-command-partition-design.md, 273 lines) and a plan doc (2026-05-31-per-command-partition.md, 813 lines), corroborating that this change warranted upfront design documentation.
- theoju/claude-code-self-assessment#108: This PR adds three new user-visible milestone detectors to the progression timeline subsystem (scheduled, remote, verification), closing a documented coverage gap (CCE-33). It touches core telemetry logic (`scripts/progression.mjs`, `scripts/_usage-data.mjs`), extends the `scanTranscriptModes` data contract, and introduces new dashboard behavior visible to end users. At 1,206 total LOC across 6 files — including a new 89-line detector block and 187-line test expansion — this is a substantive feature addition rather than a refactor or dependency bump. A senior engineer would expect a written spec and plan for a change of this scope and user-visibility. Notably, the PR already includes both (`docs/superpowers/plans/2026-05-31-cce-33-progression-detectors.md` and `docs/superpowers/specs/2026-06-01-cce-33-progression-detectors-design.md`), satisfying the expectation.
- theoju/claude-code-self-assessment#106: The PR introduces a new subsystem (`scripts/rank-next-actions.mjs`) with a formally declared public contract (`assessment.json.rankedNextActions`), collapses `evaluatePredicate` to a re-export establishing a new canonical DSL boundary, and adds a CLAUDE.md hard rule codifying the contract. While framed as a structural fix/refactor, it delivers a new pre-computation pipeline and a user-visible change to the assessment output schema — both of which a senior engineer would expect to be backed by a written design or spec before implementation.
- theoju/claude-code-self-assessment#100: This PR bootstraps a new subsystem (engineering-docs-agent plugin integration) and introduces a 143-line nightly GitHub Actions workflow alongside plugin config and state files. It is not a refactor, dependency bump, or formatting change — it adds a new automation system with infrastructure implications (scheduled CI, state management, plugin configuration). A senior engineer would expect a written spec or plan for onboarding a new agent plugin and its associated nightly pipeline. The referenced Jira tickets (CCE-57, CCE-64, etc.) suggest planning occurred, but the change is of a nature that warrants formal documentation regardless.

