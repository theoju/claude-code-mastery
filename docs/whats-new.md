## 2026-06-01T09:00:08.671058+00:00
- PR #107: Two deferred test-hardening items from the PR #106 review cycle were closed: (1) a new regression test pins the internal `axisOrder()` sort-tier mapping so that an unknown axis value sorts into tier 2 (adjacent to 'either'), not ahead of 'platform'; (2) the `evaluatePredicate` equality test was extended to assert that string-typed LHS values ('5') match a numeric literal (5), locking the `String(value) === lit` cross-type coercion contract. No production code was touched; the diff is confined to two test files.
- PR #106: PR #106 extracts a canonical predicate DSL evaluator into `scripts/predicate.mjs` (ported byte-for-byte from `app/lib/assessment.ts`) and collapses the TypeScript `evaluatePredicate` to a 2-line re-export, ensuring there is exactly one implementation. It adds `scripts/rank-next-actions.mjs`, which filters satisfied next-actions through the canonical evaluator and ranks the remainder by `weight × deficit` with a deterministic 5-tier tie-break. `scripts/run-assessment.mjs` is wired to write a pre-computed `rankedNextActions[10]` array into `app/data/assessment.json` on every `npm run assess` run. The `Assessment` TypeScript interface gains a `RankedNextAction` type and both load branches default the field to `[]`. The `/self-assessment` SKILL.md is simplified to a single bullet reading from the pre-computed array. Two CLAUDE.md hard rules pin the contracts: 'DSL evaluator has one source' and 'Ranked next-actions live in assessment.json.rankedNextActions'. Thirty new tests (predicate operators × 18, ranker × 9, wire-up × 2, passthrough equivalence × 1) are added, including a named regression asserting that `loopCommandUses=14` excludes the already-satisfied `babysit-loop` action.
- PR #104: PR 1 of 2 in a two-part fix for the recurring '/self-assessment skill re-implements the DSL filter' bug class. Adds a 12-line DSL grammar block to `.claude/skills/self-assessment/SKILL.md` documenting all 7 `satisfiedWhen` operator classes with a canonical-implementation pointer, preventing models from hand-writing broken filters. Also ships the design spec (`docs/superpowers/specs/2026-05-31-predicate-ranker-design.md`) and full implementation plan (`docs/superpowers/plans/2026-05-31-predicate-ranker.md`) for the structural PR 2 fix, which will extract `evaluatePredicate` to `scripts/predicate.mjs` and pre-compute `assessment.json.rankedNextActions`.
- PR #102: Changed `dismissed_gap_flags` in `.engineering-docs-agent/state.json` from an empty array (`[]`) to an empty object (`{}`). The plugin's state schema requires this field to be a keyed object (`{owner}/{name}#{pr}` keys), and the incorrect type caused strict schema validation to fail at orchestrator startup with exit code 2 before any subagent could dispatch.
- PR #101: PR #101 adds a new Conventions entry to CLAUDE.md documenting the `/progression` timeline's dual milestone-source architecture: telemetry detectors (`scripts/progression.mjs`) self-date from session `start_time` across full history, while config detectors (`scripts/config-progression.mjs`) stamp `firstSeenAt` at first observation and freeze it. It records the deliberate first-run caveat that causes all 8 config milestones to share the identical `2026-05-09T08:37:16.111Z` timestamp, and documents the coverage gap — 3 of 12 scored dimensions (`scheduled`, `remote`, `verification`) have no milestone detector (tracked as CCE-33). Also fixes the file-map: adds the missing `config-progression.mjs` entry and corrects the stale `app/progression/page.tsx` comment.
- PR #100: PR #100 bootstraps the engineering-docs-agent plugin on the `claude-code-self-assessment` repo. It adds `.engineering-docs-agent/config.yml` (host config using the new `framework: none` first-class value, single `core` lens at `docs/`), a seeded `state.json`, a `state.example.json` template, and a `.github/workflows/docs-agent-nightly.yml` nightly CI workflow that wires in all recent plugin fixes (CCE-45 app-token, CCE-49 OAuth validation, CCE-41 forensics upload, CCE-53 Jira). A synthetic mkdocs scaffold that was briefly present was removed after CCE-64 made `framework: none` valid, so no SSG is installed or required.
### Gaps flagged
- theoju/claude-code-self-assessment#106: This PR introduces a new canonical DSL evaluator subsystem (`scripts/predicate.mjs`), a new ranking module (`scripts/rank-next-actions.mjs`), changes the output contract of `assessment.json` by adding a `rankedNextActions` field, collapses `app/lib/assessment.ts` to a passthrough re-export, and pins two new hard architectural rules in CLAUDE.md. It touches 11 files with 658 LOC changed. This is a new architectural subsystem with a changed public output contract and user-visible behavior — exactly the class of change a senior engineer would expect a spec/plan to accompany. Notably, the PR body explicitly references a design doc and plan that were merged with the prior PR (#104), confirming the spec expectation was met.
- theoju/claude-code-self-assessment#100: This PR bootstraps a new subsystem integration (engineering-docs-agent plugin) and introduces a nightly CI/CD automation workflow. It adds a new plugin configuration, state files, and a GitHub Actions workflow that runs an agent on a schedule. These changes constitute a new subsystem with user-visible automation behavior (nightly workflow runs, Jira wiring, OAuth/App-token plumbing). A senior engineer would expect a written spec or design plan to accompany this kind of integration bootstrap — covering agent behavior, failure modes, publishing decisions, and security surface (App token, OAuth). The PR body is detailed but is itself a summary, not an upstream design document; the referenced CCE-57 Jira ticket would be the expected spec artifact.

