## 2026-06-01T15:59:26.065586+00:00
- PR #111: Two-line edit to `.github/workflows/docs-agent-nightly.yml` to follow an upstream breaking change in `actions/create-github-app-token@v3`: the deprecated `app-id` input is replaced with `client-id` (now sourced from a repo Variable `DOCS_AGENT_APP_CLIENT_ID`), and `JIRA_EMAIL` is moved from Secrets to Variables because it is a basic-auth username rather than a credential. No logic or behavior of the nightly docs-agent job changes; only how the app token is minted and how the Jira email is stored.
- PR #110: Codifies a posture-vs-volume command partition in the transcript scanner (`scripts/_usage-data.mjs`). Posture commands (`/color`, `/voice`, `/focus`, `/btw`, `/clear`, `/compact`, `/simplify`, `/rewind`, `/fewer-permission-prompts`) are now counted only from `interactive_cli` or `unknown` session kinds, preventing observer and SDK-orchestrated sessions from inflating those counters by echoing the primary session's command markup. Volume commands (`/loop`, `/schedule`, `/babysit`, `/go`, `/batch`) remain counted across every session kind. A fail-loud `assertCommandPartition` guard runs at module load to catch set-drift (non-disjoint sets, unclassified commands, dead classifications). Two new design artifacts ship with the implementation: a formal spec and a step-by-step implementation plan.
- PR #108: Closes the `/progression` timeline coverage gap (CCE-33) by adding three new telemetry-dated milestone detectors to `scripts/progression.mjs`: `scheduled` (fires on first `/loop`/`/schedule`/`/babysit` invocation, Boris tip 48), `remote` (fires on first `RemoteTrigger`/`PushNotification`/`SendMessage` tool call, tip 35), and `verification` (fires on first `/go` invocation, tip 73). Previously only 8 of 12 scored dimensions had progression coverage; this adds the three missing ones. A supporting additive change to `scripts/_usage-data.mjs::scanTranscriptModes` attaches a per-session `commands: Set<string>` field used by the two transcript-dependent detectors. The probe-tracker spec is updated in the same PR per the hard rule, and a design spec plus implementation plan are shipped alongside.
- PR #107: Pure test-hardening cleanup of two Minor findings deferred from the PR #106 predicate-ranker review cycle. Added an explicit regression test pinning the internal `axisOrder()` enum so unknown axis values sort at tier 2 (adjacent to 'either') rather than ahead of 'platform'. Extended the equality predicate test to assert string-coercion behaviour (`evaluatePredicate('x=5', { x: '5' }) === true`), locking the `String(value) === lit` contract against a future strict-equality refactor. Also drops a stale stash entry that would have conflicted with PR #106's collapse. No production code was modified.
- PR #106: Extracts a canonical DSL predicate evaluator into `scripts/predicate.mjs` and collapses `app/lib/assessment.ts:evaluatePredicate` to a thin re-export passthrough, ending the duplicate-implementation pattern. Adds `scripts/rank-next-actions.mjs` which filters out already-satisfied rubric actions via the canonical evaluator and ranks the remainder by weight×deficit (5-tier deterministic tie-break), then wires it into `scripts/run-assessment.mjs` so every `npm run assess` pre-computes a `rankedNextActions[10]` array in `app/data/assessment.json`. Simplifies `.claude/skills/self-assessment/SKILL.md` to read those pre-computed top actions instead of re-implementing filter logic. Adds two CLAUDE.md hard rules that lock the contract end-to-end ('DSL evaluator has one source' and 'Ranked next-actions live in assessment.json.rankedNextActions'). Thirty new tests added, including a named regression pinning the originating production bug. TypeScript `Assessment` interface gains a `RankedNextAction` type and the new field.
- PR #104: PR 104 is a two-part tactical fix for the recurring 'model re-implements the DSL filter' bug class in the /self-assessment skill. Part 1 (this PR) adds a 12-line satisfiedWhen DSL grammar block to .claude/skills/self-assessment/SKILL.md documenting all 7 operator classes with a pointer to the canonical evaluator, and ships the design spec (docs/superpowers/specs/2026-05-31-predicate-ranker-design.md) and implementation plan (docs/superpowers/plans/2026-05-31-predicate-ranker.md) for the structural fix that will follow in PR 2.
- PR #102: Fixed a one-character schema error in .engineering-docs-agent/state.json: the dismissed_gap_flags field was initialized as an empty JSON array ([]) instead of an empty JSON object ({}). The plugin's state schema requires this field to be an object keyed by {owner}/{name}#{pr} strings. The incorrect value caused the orchestrator to exit with code 2 during strict schema validation before any subagent could dispatch, blocking the docs-agent nightly run entirely.
- PR #101: Adds a Conventions entry to CLAUDE.md documenting the `/progression` timeline's dual milestone-source architecture: telemetry detectors in `scripts/progression.mjs` self-date from session `start_time` across full history, while config detectors in `scripts/config-progression.mjs` stamp `firstSeenAt` at first observation and freeze it. Records the deliberate first-run caveat (all 8 config milestones share the identical `2026-05-09T08:37:16.111Z` timestamp) and the coverage gap where `scheduled`, `remote`, and `verification` dimensions have no milestone detector (tracked as CCE-33). Also fixes the file-map: adds the missing `config-progression.mjs` entry and corrects the stale comment on `app/progression/page.tsx`.
- PR #100: Bootstraps the `claude-code-self-assessment` repository onto the engineering-docs-agent plugin. Adds the host configuration file (`.engineering-docs-agent/config.yml`) using `framework: none` (enabled by CCE-64), seeded state files, and a nightly GitHub Actions workflow (`.github/workflows/docs-agent-nightly.yml`) that wires in CCE-45 App-token auth, CCE-49 OAuth validation, CCE-41 forensics upload, and CCE-53 Jira integration. A previously planned synthetic mkdocs scaffold (`mkdocs.yml`, `requirements-docs.txt`) was removed after `framework: none` became a first-class config value, so the repo now accurately reflects its plain-markdown-on-GitHub shape with no SSG.
### Gaps flagged
- theoju/claude-code-self-assessment#110: This PR introduces a new posture-vs-volume command partition that changes user-visible scoring behavior (posture counters, threshold crossings that affect dimension scores) and adds new public API surfaces (`POSTURE_COMMANDS`, `VOLUME_COMMANDS`, `assertCommandPartition`). It modifies a core transcript-scanning algorithm in `scripts/_usage-data.mjs` across 83 additions and 10 deletions. A senior engineer would clearly expect a written spec and plan to accompany an architectural change of this type. The PR already includes both: `docs/superpowers/specs/2026-05-31-per-command-partition-design.md` (+273 lines) and `docs/superpowers/plans/2026-05-31-per-command-partition.md` (+813 lines), confirming the expectation is met rather than flagging a gap.
- theoju/claude-code-self-assessment#108: This PR adds three new milestone detectors to a core scoring subsystem (scripts/progression.mjs) and extends scanTranscriptModes with a new per-session field, closing a documented coverage gap (CCE-33). It introduces new user-visible behavior on the /progression timeline for three previously uncovered dimensions (scheduled, remote, verification). This is a new subsystem capability — not a refactor, dependency bump, or formatting change — and a senior engineer would expect a written spec or plan. Notably, the PR itself confirms this expectation: it ships a 235-line design spec and a 619-line implementation plan alongside the code changes.
- theoju/claude-code-self-assessment#106: This PR introduces a new canonical DSL evaluator subsystem (scripts/predicate.mjs), a new ranking module (scripts/rank-next-actions.mjs), changes the assessment.json public contract by adding a rankedNextActions field, and establishes new hard rules in CLAUDE.md. It is a structural architectural fix to a recurring bug class, touches multiple layers (scripts, app/lib, assessment pipeline, SKILL.md, docs), and adds 658 LOC across 11 files. A senior engineer would expect a written spec and plan for this scope — and the PR body confirms both exist (design spec and plan merged with PR #104).
- theoju/claude-code-self-assessment#100: This PR bootstraps a new external automation subsystem (engineering-docs-agent plugin) onto the repository, introducing a nightly CI/CD workflow and multi-stage agent pipeline configuration. It represents a new subsystem integration — not a refactor, dependency bump, or formatting change — with materially new operational behavior (nightly runs, external agent stages, state management). A senior engineer would expect a written spec or plan covering the agent integration contract, CI workflow design, and capability tradeoffs (framework=none degradation). The detailed PR body partially serves this role and references CCE-57, but that itself implies a Jira-backed plan exists; the change warrants formal spec accompaniment.

