# What's New

The engineering-docs-agent appends entries here on each nightly run
when it detects merged work worth surfacing in a user-facing
changelog. Most entries are written by the agent and reviewed via
the `docs-agent/YYYY-MM-DD` PR.

<!-- The first authored entry will land below on the first nightly
     after the mkdocs upgrade ships. -->
## 2026-06-18T08:45:39.745829+00:00
- PR #101: PR #101 adds a Conventions entry to CLAUDE.md documenting the `/progression` timeline's dual milestone-source architecture: telemetry milestones (`scripts/progression.mjs`) self-date from session `start_time` over full history, while config milestones (`scripts/config-progression.mjs`) stamp `firstSeenAt` at first observation and freeze it. It records the deliberate first-run caveat that causes all 8 config milestones to share the identical `2026-05-09T08:37:16.111Z` timestamp, and documents the coverage gap — 3 of 12 scored dimensions (`scheduled`, `remote`, `verification`) have no milestone detector, tracked as CCE-33. Also fixes two stale entries in the file-map: adds the missing `config-progression.mjs` row and corrects the `app/progression/page.tsx` comment to note it reads `app/data/progression.json` rather than `/insights` history.
- PR #100: Onboards the claude-code-self-assessment repo onto the engineering-docs-agent plugin by adding a host config file (.engineering-docs-agent/config.yml) with framework: none and a single core lens at docs/, a seeded state.json, a state.example.json placeholder template, and a nightly GitHub Actions workflow (.github/workflows/docs-agent-nightly.yml) that runs the full docs-agent pipeline (source-collector, pr-summarizer, page-author, content-validator, gap-detector, notifier) while skipping the framework build lint and publish-verifier stages that do not apply under framework: none.
- PR #102: Fixed a one-character JSON error in `.engineering-docs-agent/state.json`: the `dismissed_gap_flags` field was seeded as an empty array (`[]`) by the bootstrap PR instead of an empty object (`{}`). The plugin's state schema requires an object keyed by `{owner}/{name}#{pr}`, so strict schema validation caused the orchestrator to exit with code 2 within ~0.2s of startup, blocking all nightly docs-agent runs before any subagent could dispatch.
- PR #104: PR 1 of 2 for the predicate-ranker fix. Adds a 12-line DSL grammar block to the /self-assessment SKILL.md documenting all 7 satisfiedWhen operator classes with a pointer to the canonical evaluator (app/lib/assessment.ts:evaluatePredicate), preventing models from re-implementing the string DSL filter from scratch. Also ships the design spec and implementation plan that blueprint the structural fix arriving in PR 2: extracting evaluatePredicate to a Node-shareable scripts/predicate.mjs and pre-computing filtered/ranked top-N next-actions into assessment.json.rankedNextActions.
- PR #106: Extracted a canonical `scripts/predicate.mjs` DSL evaluator (ported byte-for-byte from `app/lib/assessment.ts`) and added `scripts/rank-next-actions.mjs`, which filters satisfied next-actions via the canonical evaluator and ranks the remainder by weight×deficit with a deterministic 5-tier tie-break. `scripts/run-assessment.mjs` now writes a pre-computed `rankedNextActions[10]` array into `app/data/assessment.json` on every `npm run assess` run. `app/lib/assessment.ts:evaluatePredicate` was collapsed to a 2-line re-export of the canonical module, and a CI equivalence test enforces reference identity so the implementation can never be duplicated. The `Assessment` TypeScript interface was extended with a `RankedNextAction[]` field. `.claude/skills/self-assessment/SKILL.md` was simplified to a single instruction to read the pre-computed list, and two hard rules were added to `CLAUDE.md` pinning the DSL-single-source and pre-computed-ranking contracts.
- PR #107: Pure test-hardening follow-up to PR #106. Adds a regression test that pins the internal `axisOrder()` sort mapping so an unknown axis value stays in tier 2 (adjacent to 'either') rather than accidentally jumping ahead of 'platform'. Also extends the existing predicate equality test to cover cross-type string/number coercion (`evaluatePredicate("x=5", { x: "5" })` must return `true`), locking the `String(value) === lit` contract. A superseded WIP stash entry is dropped. No production code was modified.
- PR #108: Added three telemetry-dated milestone detectors to the /progression timeline — `scheduled` (Boris tip 48, fires on first `/loop`/`/schedule`/`/babysit` invocation), `remote` (tip 35, fires on first `RemoteTrigger`/`PushNotification`/`SendMessage` tool fire), and `verification` (tip 73, fires on first `/go`). Also extended `_usage-data.mjs::scanTranscriptModes` with a per-session `commands: Set<string>` field to support the transcript-derived detectors. The probe-tracker spec was updated in the same PR with a new Progression layer (3 rows) and re-derived header counts (unchanged: 75/12/48/47/71). Closes the coverage gap where only 8 of 12 scored dimensions had milestone detectors; all three new milestones fire with backdated telemetry timestamps (scheduled @ 2026-04-29, remote @ 2026-04-15, verification @ 2026-05-26). 622/622 tests pass.
- PR #110: Introduced a canonical posture-vs-volume command partition in the transcript scanner (`scripts/_usage-data.mjs`). Posture commands (/color, /voice, /focus, /btw, /clear, /compact, /simplify, /rewind, /fewer-permission-prompts) are now counted only when the session kind is `interactive_cli` or `unknown`, eliminating false positives previously caused by observer and SDK-orchestrated sessions echoing the primary session's command markup. Volume commands (/loop, /schedule, /babysit, /go, /batch) remain counted across all session kinds. A fail-loud `assertCommandPartition` assertion runs at module load to catch partition drift. Two posture predicates (/simplify and /rewind thresholds) dropped to zero as the spec predicted — confirmed as inflation removal, not regression. 11 new tests were added and 633/633 pass.
- PR #111: Migrated the docs-agent-nightly GitHub Actions workflow from the deprecated `app-id` field to `client-id` in `actions/create-github-app-token@v3`, and moved `JIRA_EMAIL` from repository secrets to repository variables, since it is a basic-auth username rather than a sensitive credential.
- PR #113: Introduced a pure helper `stageRanInEntry(entry, legacyNumber, newName)` in `scripts/signals.mjs` that collapses detection of `/ship` Stage 2 (verify-agent) and Stage 3 (simplify) across all three `~/.claude/ship/journal.jsonl` format generations (singular `entry.stage`, legacy-numeric `stages_run`, new-string `stages_run`) into a single strict-equality `Array.includes` check. `gatherShipJournal` was widened to use this helper for both `stage2Count` and a new `simplifyStageCount` counter. `run-assessment.mjs` MAX-merges the new `simplifyStageCount` into the `simplifyCommandUses` projection, mirroring the v0.9.16 `/color` pattern. The journal lookback at the production call site was also widened from 14 to `insightsLookbackDays` (default 30 days) to align with transcript-derived signals. The probe-tracker spec was annotated with a `[^journal-stage-credit]` footnote, and a CLAUDE.md Conventions bullet documents the `stageRanInEntry` pattern and canonical stage 0–7 mapping.
- PR #114: Four shipped plan documents were moved from `docs/superpowers/plans/` into `docs/superpowers/plans/archived/` — a pure rename/file-move with no content changes. The four plans correspond to features already landed in PRs #94, #108, #110, and #104.
- PR #116: PR #116 (CCE-76) completes Execution scorer coverage for all twelve scoring dimensions by replacing the two remaining `noTelemetry()` stubs — Memory & Context Management and Terminal & Customization — with real ratio scorers. The new scorers use `withGates({ transcripts: true, universe: "interactive_or_unknown" })`, consuming transcript-derived posture-command session-coverage counters (memory: `/btw`, `/clear`, `/compact`, `/rewind`; customization: `/color`, `/voice`, `/focus`). Two counter fields (`focusCommandUses`, `rewindCommandUses`) are unified from per-message to per-session-coverage increments to match the canonical pattern. A new `interactiveOrUnknownSessionsAnalyzed` denominator signal and `interactive_or_unknown` universe option are introduced to satisfy the numerator-must-be-subset-of-denominator hard rule established in PR #97. The methodology page is updated to document the new measurement basis for both dimensions, and the probe-tracker spec is annotated for the seven affected transcript-counter rows.
- PR #117: Release v0.9.18 bundles 13 PRs landed since v0.9.17 (2026-05-26). Key headlines: (1) Memory and Terminal & Customization Execution scorers replace noTelemetry() stubs with transcript-gated ratio scorers, completing Execution coverage across all 12 dimensions; (2) per-command POSTURE/VOLUME partition prevents observer/SDK sessions from inflating posture counters, with a fail-loud boundary assertion at module load; (3) /ship Stage 2 (verify-agent) and Stage 3 (simplify) dispatch counters now handle all three journal format generations; (4) canonical predicate evaluator consolidated to scripts/predicate.mjs with assessment.ts as a 1-line passthrough and pre-computed rankedNextActions in assessment.json; (5) /progression timeline gains telemetry-dated detectors for scheduled, remote, and verification dimensions. Supporting changes include the engineering-docs-agent plugin onboarding, a state.json shape fix, and Jira workflow migration to client-id auth.
- PR #118: Archived two shipped implementation plans — CCE-72 (ship-journal stage credit across journal formats, PR #113) and CCE-76 (Memory + Customization Execution scorers, PR #116) — by moving them from `docs/superpowers/plans/` into `docs/superpowers/plans/archived/` via a mechanical `git mv`. No content was changed.
- PR #119: Fixed a counter-semantics corruption in the scoring layer: `signalsSummary.btwCommandUses` previously Math.max-blended the cumulative all-time `~/.claude.json#btwUseCount` with the 30-day windowed session-coverage counter, conflating two independent axes — (a) time window (windowed vs. cumulative) and (b) counter class (per-session-coverage vs. raw invocation count). The fix splits the field: `btwCommandUses` now reflects only the windowed session-coverage signal, while a new `cliBtwUseCountAllTime` field surfaces the cumulative count separately. The rubric predicate for `btw-side-channel` (Boris tips 33 and 54) was rerouted from `btwCommandUses>=1` to `cliBtwUseCountAllTime>=1`, matching the true semantic of the 'have you ever adopted this habit' check. A hard rule documenting the cumulative-vs-windowed counter discipline was added to CLAUDE.md. Memory Execution score is unchanged (16); the corruption existed only on the signalsSummary surface.
- PR #120: Version bump to v0.9.19 bundling two previously merged PRs: (1) a scoring-honesty fix (CCE-78) that unblends the cumulative all-time /btw invocation count from the 30-day windowed btwCommandUses session-coverage signal in signalsSummary, exposing the cumulative count as a new cliBtwUseCountAllTime field and rerouting the btw-side-channel predicate (tips 33+54) to it; and (2) housekeeping that archives the CCE-72 and CCE-76 landed plans from the active plans directory.
- PR #121: Upgrades the engineering-docs-agent integration from `framework: none` to `framework: mkdocs`, standing up a published Material-theme docs site at https://theoju.github.io/claude-code-self-assessment/. The change adds two CI workflows (a push-triggered Pages deploy workflow and a PR-level `mkdocs build --strict` gate), migrates the existing flat `docs/*.md` files verbatim into `docs/site-src/`, repairs 9 broken relative links caused by the path move, flips five fields in `.engineering-docs-agent/config.yml` to activate the publish-verifier stage of the nightly agent, and updates path references across 6 additional files. Three new vitest files (21 cases) cover path migration, scaffold existence/content, and config contract validation.
- PR #122: Post-merge follow-up to PR #121 (CCE-81 mkdocs upgrade) recording three durable lessons from the actual GitHub Pages deploy incident: (1) `actions/configure-pages@v6 enablement: true` does NOT self-bootstrap Pages on first deploy — the workflow GITHUB_TOKEN lacks admin scope; the fix is a one-time `gh api -X POST .../pages -f build_type=workflow` call; (2) monitor scripts must avoid zsh read-only built-ins (`status`, `pipestatus`) — assigning to either crashes the shell silently, making script failure look like watched-system failure; (3) all three deviations from the planned rollout are now recorded in the spec and plan docs as dated post-implementation corrections. Three docs files changed: CLAUDE.md (two new convention bullets), the mkdocs upgrade spec (POST-IMPLEMENTATION CORRECTION block under Gate 5), and the mkdocs upgrade plan (post-merge outcomes section).
- PR #125: Dropped the misleading `enablement: true` field from the `docs-agent-pages.yml` GitHub Actions workflow (it was always a no-op after Pages was manually bootstrapped during the CCE-81 incident), flipped the matching vitest assertion to a regression guard that prevents the line from being re-added, appended a one-line resolution footer to the existing POST-IMPLEMENTATION CORRECTION block in the mkdocs upgrade spec, and shortened the CLAUDE.md gotcha bullet to point at the plugin's CLAUDE.md as the canonical explanation.
- PR #127: Adds a new convention bullet to CLAUDE.md (all three sibling repos simultaneously) stating that plan-step verification must invoke the actual consumer tool — mkdocs build --strict, npx tsc --noEmit, ajv validate, etc. — rather than relying solely on filesystem presence checks such as test -f. The rule is byte-identical across this repo, theoju/advanced-data-import-system, and theoju/engineering-docs-agent (MD5 c211304951e64e7a3bea48fbf923ab28). No source code or test suites are affected; the change is purely a documented development convention.

