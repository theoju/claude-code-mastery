# What's New

The engineering-docs-agent appends entries here on each nightly run
when it detects merged work worth surfacing in a user-facing
changelog. Most entries are written by the agent and reviewed via
the `docs-agent/YYYY-MM-DD` PR.

<!-- The first authored entry will land below on the first nightly
     after the mkdocs upgrade ships. -->
## 2026-07-13T08:14:27.966740+00:00
- PR #101: Documents the dual milestone-source architecture behind the /progression timeline: telemetry-derived milestones (scripts/progression.mjs) self-date from session start_time over full history, while config-derived milestones (scripts/config-progression.mjs) stamp firstSeenAt at first-observed-run and freeze it thereafter. Also records that 3 of 12 scored dimensions (scheduled, remote, verification) currently have no milestone detector, so real adoption in those areas never surfaces on the timeline, and fixes two stale file-map references (missing config-progression.mjs entry, incorrect comment on app/progression/page.tsx).
- PR #100: Bootstrapped the claude-code-self-assessment repo onto the engineering-docs-agent plugin: added `.engineering-docs-agent/config.yml` (single `core` lens at `docs/`, `framework: none`, publishing fields nulled out), seeded `state.json` / `state.example.json`, and added a dogfood-derived `.github/workflows/docs-agent-nightly.yml` (no mkdocs install step, since framework=none needs none). An earlier synthetic `mkdocs.yml` + `requirements-docs.txt` scaffold was removed in a follow-up commit once CCE-64 landed `framework: none` as a first-class config value, so the host config now reflects the repo's real shape (plain markdown rendered by GitHub, no SSG).
- PR #102: Corrected a one-character JSON type error in the engineering-docs-agent plugin's state file: `dismissed_gap_flags` was seeded as an empty array (`[]`) but the plugin's state schema requires it to be an object (keyed by `{owner}/{name}#{pr}`). The malformed field caused strict schema validation to fail and the orchestrator to exit with code 2 within ~0.2s of starting, before any subagent could dispatch.
- PR #104: Added a 12-line grammar block to the `/self-assessment` skill documenting all 7 `satisfiedWhen` operator classes used by the predicate DSL, with a pointer to the canonical evaluator (`app/lib/assessment.ts:evaluatePredicate`) and a worked example. Also shipped the design spec and implementation plan for a follow-up structural fix (a dedicated predicate-ranker module) that will later replace this grammar block once implemented.
- PR #106: Extracted a single canonical predicate DSL evaluator (scripts/predicate.mjs) used by both the assessment scorer and the TypeScript dashboard (app/lib/assessment.ts now re-exports it instead of duplicating logic), and added a rank-next-actions module that pre-computes a ranked top-10 list of unsatisfied next-actions (filtered via the canonical evaluator, sorted by weight×deficit with a deterministic tie-break) directly into assessment.json.rankedNextActions on every `npm run assess` run. The self-assessment SKILL.md was simplified to just read the pre-computed list instead of re-implementing the satisfiedWhen filter, and the Assessment TypeScript interface was updated to expose the new field.
- PR #107: Added regression test coverage for two deferred Minor review findings from the predicate-ranker PR #106 cycle: a test pinning the internal axisOrder() tier mapping so an unknown axis value sorts into tier 2 alongside 'either' rather than ahead of 'platform', and an extension of the equality predicate test asserting evaluatePredicate("x=5", { x: "5" }) returns true to lock in the String(value) === lit cross-type-coercion contract. Also dropped a stale git stash entry superseded by what PRs #104 and #106 actually shipped.
- PR #108: Adds three new telemetry-dated milestone detectors to the /progression timeline — 'scheduled' (fires on first /loop, /schedule, or /babysit), 'remote' (fires on first RemoteTrigger/PushNotification/SendMessage tool use), and 'verification' (fires on first /go) — closing the coverage gap where those three of twelve scored dimensions previously never produced a progression milestone. Also extends the transcript scanner (scanTranscriptModes) with a new per-session commands: Set<string> field, purely additive, to support the two transcript-derived detectors. The probe-implementation-status tracker spec was updated in the same PR per project convention, adding a Progression layer with three new rows; no new satisfiedWhen predicates, probe-catalog entries, or signalsSummary keys were introduced.
- PR #110: Introduces a per-command partition (`POSTURE_COMMANDS` vs `VOLUME_COMMANDS`) in the transcript-scanning layer so that posture-oriented slash commands (`/color`, `/voice`, `/focus`, `/btw`, `/clear`, `/compact`, `/simplify`, `/rewind`, `/fewer-permission-prompts`) are only counted from transcripts belonging to `interactive_cli` or `unknown` session kinds, while volume commands (`/loop`, `/schedule`, `/babysit`, `/go`, `/batch`) continue to be counted across all session kinds. A fail-loud `assertCommandPartition` check runs at module load to catch future drift (disjointness, missing/dead classification).
- PR #111: Updated `.github/workflows/docs-agent-nightly.yml` to use `client-id: ${{ vars.DOCS_AGENT_APP_CLIENT_ID }}` instead of the deprecated `app-id: ${{ secrets.DOCS_AGENT_APP_ID }}` for GitHub App token generation, and moved `JIRA_EMAIL` from a repo Secret to a repo Variable since it's a basic-auth username rather than a credential.
- PR #113: Widened the self-assessment scorer's ship-journal detection so that /ship Stage 2 (verify-agent) and Stage 3 (simplify) dispatches are credited across all three historical `~/.claude/ship/journal.jsonl` format generations, not just the original singular `entry.stage === 2` shape. A new pure helper `stageRanInEntry(entry, legacyNumber, newName)` centralizes the three-format detection with strict-equality `Array.includes` checks. `gatherShipJournal` now also returns a `simplifyStageCount`, which `run-assessment.mjs` MAX-merges into the `simplifyCommandUses` projection (mirroring the existing /color pattern). The production call site's lookback window was also widened from a fixed 14 days to the configurable `insightsLookbackDays` (default 30) so journal-derived signals align with transcript-derived ones.
- PR #114: Moved four already-shipped planning documents from the active plans directory into an archived subfolder, with no content changes — a pure file relocation to keep the plans directory reflecting only in-progress work.
- PR #116: Replaced the placeholder `noTelemetry()` handling for the Memory & Context Management and Terminal & Customization Execution scorers with real transcript-derived ratio scorers gated on a new `interactive_or_unknown` universe (interactive_cli ∪ unknown sessions). `/focus` and `/rewind` counters were unified from per-message to per-session (session-coverage) counting to match the existing `/btw`, `/clear`, `/compact` pattern, and a new `interactiveOrUnknownSessionsAnalyzed` denominator signal was added so the ratio's numerator stays a subset of its denominator. As a result all twelve dashboard dimensions now report a measured Execution score — Memory and Customization move from italic 'unmeasured' to numeric scores (16 and 3 respectively) with evidence strings, and the overall Execution composite correctly drops from 77 to 66 because two previously-excluded low-scoring dims now enter the average.
- PR #117: Release chore bumping package.json to v0.9.18. No code changes in this PR itself, but it marks the release point for 13 prior PRs merged since v0.9.17: all 12 rubric dimensions now have Execution scorers (Memory & Customization added via withGates ratio scorers over MAX-merged transcript+history posture-command counters), a fail-loud POSTURE/VOLUME command partition prevents observer/SDK sessions from inflating posture counters, /ship Stage 2/3 dispatch counting now spans all three ship-journal format generations, a canonical predicate evaluator and pre-computed rankedNextActions landed in assessment.json, and the /progression timeline gained telemetry-dated detectors for scheduled/remote/verification.
- PR #118: Two already-shipped implementation plans (CCE-72's /ship journal stage-credit work and CCE-76's Memory + Customization Execution scorers) were moved via mechanical `git mv` from the active plans directory into `docs/superpowers/plans/archived/`, with no content changes.
- PR #119: Fixed a scoring-semantics bug in the Memory Execution dimension: `signalsSummary.btwCommandUses` no longer Math.max-blends a cumulative all-time counter (`~/.claude.json#btwUseCount`) into what should be a strictly 30-day windowed session-coverage metric. Introduced a new `cliBtwUseCountAllTime` field on `signalsSummary` to carry the cumulative signal separately, and rerouted the rubric's `btw-side-channel` predicate (Boris tips 33/54) from `btwCommandUses>=1` to `cliBtwUseCountAllTime>=1`, since the cumulative field is the truer signal for a 'have you ever adopted this habit' check. Updated the probe catalog, rubric, probe-implementation-status tracker counts, and added regression tests. Also added a new CLAUDE.md hard rule generalizing the two semantic axes (time window: windowed vs cumulative; counter class: session-coverage vs raw invocation count) that a numerator field must be checked against before being folded into a ratio, to prevent the same blend pattern from recurring in future scorers.
- PR #120: Bumped the claude-code-self-assessment package version from 0.9.18 to 0.9.19, cutting a release that bundles two prior merges: a fix that unblends the cumulative `~/.claude.json` `btwUseCount` from the 30-day windowed `signalsSummary.btwCommandUses` (exposing the cumulative figure separately as `cliBtwUseCountAllTime` and rerouting the `btw-side-channel` rubric predicate to it), and a housekeeping PR archiving landed CCE-72/CCE-76 plans.
- PR #121: Upgrades the engineering-docs-agent integration for this repo from framework: none to framework: mkdocs. Stands up a published Material-theme docs site at https://theoju.github.io/claude-code-self-assessment/, adds a PR-level `mkdocs build --strict` gate (docs-build-check.yml) plus a push-to-main deploy workflow (docs-agent-pages.yml via configure-pages + upload-pages-artifact + deploy-pages), and migrates the existing flat docs/*.md files into docs/site-src/ (git mv, with 9 broken relative links repaired and cross-tree refs to .claude/ and docs/superpowers/specs/ rewritten to absolute GitHub blob URLs since those paths sit outside the new docs_dir). Flips five fields in .engineering-docs-agent/config.yml (framework, whats_new_file, lens_paths.core, publishing.base_url, publishing.build_workflow), which activates the publish-verifier stage of the nightly. Six additional files (README.md, CLAUDE.md, rubric.json, ship-pattern page.tsx, self-assessment command + skill) get path references retargeted to the new docs/site-src/ location. Adds 21 new test cases across 3 vitest files covering path migration, scaffold integrity, and config contract.
- PR #122: Post-merge documentation follow-up to PR #121/CCE-81 (mkdocs upgrade rollout). Adds two new CLAUDE.md Conventions entries capturing operational gotchas discovered during the actual GitHub Pages deploy: (1) `actions/configure-pages@v6`'s `enablement: true` does not bootstrap Pages on a first deploy because the workflow token lacks admin scope, and the fix is a one-time `gh api -X POST .../pages -f build_type=workflow` call; (2) monitor scripts must avoid zsh's read-only `status`/`pipestatus` built-ins, since assigning to them crashes the shell and silently masks successful deploys. Also appends a 'POST-IMPLEMENTATION CORRECTION' block to the mkdocs-upgrade design spec under Gate 5, and a 'Post-merge outcomes' section to the mkdocs-upgrade plan documenting three deviations from the planned rollout (Pages bootstrap failure + recovery, late CCE-81 ticket filing, zsh monitor crash). No code or test changes; docs-only, +133/-0 across three files.
- PR #125: Removed the misleading, no-op `enablement: true` line from the GitHub Pages deploy workflow (docs-agent-pages.yml) and corrected the accompanying comment block that made the same wrong claim. Flipped the matching vitest assertion into a regression guard against re-adding the line, added a one-line "Resolved by" footer to the existing POST-IMPLEMENTATION CORRECTION block in the mkdocs-upgrade spec, and shortened the CLAUDE.md Pages-enablement gotcha bullet to point at the plugin's CLAUDE.md as the durable source of truth.
- PR #127: Added a new convention to CLAUDE.md requiring that plan-step verification use the actual consumer tool (e.g. `mkdocs build --strict`, `tsc --noEmit`, `ajv validate`) rather than a filesystem check like `test -f`, when a plan step produces an artifact consumed by another tool. The rule text is byte-identical across three sibling repos (advanced-data-import-system, engineering-docs-agent, and this repo).
### Gaps flagged
- theoju/claude-code-self-assessment#100: Allowlist is empty (no match) and size_filter thresholds are unset/empty, so no early-exit tier applies — falls to LLM judgment on the middle tier. This PR bootstraps a new automated subsystem onto the host repo: a nightly GitHub Actions workflow (docs-agent-nightly.yml) that runs App-token-authenticated CI, writes Jira comments/transitions, and opens PRs autonomously, plus new committed state/config files establishing a framework=none operating mode. This is exactly the class of change (new subsystem, security-relevant automation with token/Jira write access, ongoing user-visible workflow behavior) a senior engineer would expect a spec or plan to accompany, even though the PR body references upstream tickets (CCE-57, CCE-64) as the design record rather than an in-repo spec.
- theoju/claude-code-self-assessment#106: No allowlist match and size_filter thresholds are unset (empty config), so this falls to LLM judgment. The PR introduces a new canonical subsystem (scripts/predicate.mjs as the sole DSL evaluator) and a new production data contract (scripts/rank-next-actions.mjs generating assessment.json.rankedNextActions, consumed by the self-assessment skill), plus a behavior-changing refactor collapsing app/lib/assessment.ts to a passthrough. This is exactly the class of change (new subsystem + new consumer-facing data contract, fixing a recurring correctness bug) a senior engineer would expect to be backed by a written spec/plan — which in fact it was (docs/superpowers/specs/2026-05-31-predicate-ranker-design.md and the matching plan, referenced in the PR body as merged alongside sibling PR #104).
- theoju/claude-code-self-assessment#108: Adds three new telemetry-dated milestone detectors (scheduled/remote/verification) to the progression subsystem and extends the transcript-scanning data contract (_usage-data.mjs) with a new per-session field — a new-subsystem-coverage change a senior engineer would expect a written spec/plan for. No allowlist match and size thresholds are unset in config, so this falls to LLM judgment. Notably the PR itself already ships a design spec and implementation plan, confirming this expectation.
- theoju/claude-code-self-assessment#110: No allowlist match and size_filter thresholds are unset in config, so this falls to LLM judgment. The change alters scoring/telemetry classification logic (posture-vs-volume command partition) that directly affects Execution dimension scores surfaced on a user-facing dashboard, introduces a new fail-loud invariant (assertCommandPartition), and modifies documented hard-rule behavior in CLAUDE.md. This is exactly the class of change (subsystem behavior change affecting scoring output) a senior engineer would expect a written spec/plan for — which the PR author evidently agreed with, since it already references a validated spec and plan.
- theoju/claude-code-self-assessment#113: No allowlist match and size_filter thresholds are unset (empty config), so the size-filter short-circuit doesn't apply — falls to LLM judgment. The change alters core scoring logic (journal-entry stage detection across three format generations, widened lookback window, new counter feeding a user-visible next-actions ranking), which is exactly the class of behavior-affecting change a senior engineer would want a written spec/plan for before merging, not a pure refactor or formatting change. Consistent with the PR itself shipping both a design spec and implementation plan.
- theoju/claude-code-self-assessment#116: No allowlist paths configured and size_filter thresholds are unset (empty object), so neither the allowlist nor size-filter tiers apply — falls to LLM judgment. The PR introduces two new Execution scorers (Memory & Customization), a new session universe (interactive_or_unknown), a new denominator signal, and a counter-class unification (per-message → per-session) that changes user-visible dashboard scores (Execution composite -11pts) and next-action rankings. This is exactly the class of change (new subsystem behavior, user-visible score/behavior shift, cross-cutting universe/denominator semantics) a senior engineer would expect a written spec/plan for — and notably the PR itself already cites one, consistent with that expectation.
- theoju/claude-code-self-assessment#121: No allowlist_paths configured and size_filter thresholds are unset, so neither the allowlist nor size-filter tier applies — falls to LLM judgment. The PR introduces a new subsystem (mkdocs framework + Material theme docs site), stands up two new CI/CD workflows (GitHub Pages deploy + PR-level build gate), flips a host config from framework:none to framework:mkdocs, and migrates/rewrites cross-repo links — a user-visible infrastructure change with real rollout/rollback risk. A senior engineer would expect a written spec/plan for this class of change (and indeed one was produced: docs/superpowers/specs/2026-06-01-mkdocs-upgrade-design.md + the paired plan).

