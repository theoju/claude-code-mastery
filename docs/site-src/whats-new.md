# What's New

The engineering-docs-agent appends entries here on each nightly run
when it detects merged work worth surfacing in a user-facing
changelog. Most entries are written by the agent and reviewed via
the `docs-agent/YYYY-MM-DD` PR.

<!-- The first authored entry will land below on the first nightly
     after the mkdocs upgrade ships. -->
## 2026-06-14T08:32:33.823437+00:00
- PR #101: Added a Conventions entry to CLAUDE.md documenting the /progression timeline's dual-source milestone architecture: telemetry milestones (scripts/progression.mjs) are self-dated from session start_time over full history, while config milestones (scripts/config-progression.mjs) stamp firstSeenAt at first-observed and freeze it. The entry records the deliberate first-run caveat explaining why all 8 config milestones share the identical 2026-05-09T08:37:16.111Z timestamp, the coverage gap where 3 of 12 scored dimensions (scheduled, remote, verification) have no milestone detector (tracked as CCE-33), and corrects stale file-map entries for config-progression.mjs and app/progression/page.tsx.
- PR #100: Onboards the claude-code-self-assessment repository onto the engineering-docs-agent plugin. Adds the agent host config (.engineering-docs-agent/config.yml) using framework: none (plain GitHub-rendered markdown, no static-site generator), seeds the agent state file, adds a state example template, and wires a nightly GitHub Actions workflow that runs the full docs-agent pipeline. The original synthetic mkdocs scaffold was removed after CCE-64 landed framework: none as a first-class config value. Most pipeline stages (source-collector, pr-summarizer, page-author, content-validator, gap-detector, notifier) run normally under this config; framework build linting and publish verification skip cleanly.
- PR #102: Changed `dismissed_gap_flags` in `.engineering-docs-agent/state.json` from an empty array (`[]`) to an empty object (`{}`). The plugin's state schema requires this field to be a keyed object (`{owner}/{name}#{pr}` → flag), and the incorrect array type caused the orchestrator to exit with code 2 immediately on schema validation, blocking all nightly docs-agent runs.
- PR #104: Added a 12-line DSL grammar block to the /self-assessment skill documentation covering all 7 satisfiedWhen operator classes (with a canonical-implementation pointer to app/lib/assessment.ts:evaluatePredicate and a worked example). Also introduced the design spec and implementation plan for the predicate-ranker structural fix (PR 2 of 2), which will pre-compute ranked next-actions into assessment.json.rankedNextActions so no future model run needs to re-implement the DSL filter.
- PR #106: Extracts a canonical predicate DSL evaluator (`scripts/predicate.mjs`) from `app/lib/assessment.ts`, collapsing the TypeScript export to a 2-line re-export and enforcing reference-identity equivalence in CI. Adds `scripts/rank-next-actions.mjs` which filters satisfied next-actions via the canonical evaluator and ranks the remainder by weight×deficit with a 5-tier deterministic tie-break. Wires the ranker into `scripts/run-assessment.mjs` so every `npm run assess` writes a pre-computed `rankedNextActions[10]` array into `app/data/assessment.json`. Simplifies `.claude/skills/self-assessment/SKILL.md` to read from that pre-computed array rather than re-implementing the DSL. Adds `RankedNextAction` interface and `rankedNextActions` field to the TypeScript `Assessment` type. Two CLAUDE.md hard rules pin the contract end-to-end. Includes 30 new tests across 4 test files, including a named regression for the `loopCommandUses=14` / `babysit-loop` production bug.
- PR #107: Pure test hardening with no production code changes. Two deferred Minor findings from the PR #106 review cycle were closed: (1) an explicit regression test was added to pin the internal `axisOrder()` enum so a future refactor flipping the platform/execution/else sort order fails loudly rather than silently re-ranking the dashboard; and (2) the `=` equality predicate test was extended to assert that string-typed LHS values (e.g. from JSON.parse round-trips) satisfy the cross-type-coercion contract alongside the already-covered numeric case. A stale stash entry superseded by PRs #104 and #106 was also dropped.
- PR #108: Adds three new telemetry-dated milestone detectors to `scripts/progression.mjs`, closing the CCE-33 `/progression` timeline coverage gap. The new detectors cover the `scheduled` dimension (Boris tip 48, fires on first `/loop`/`/schedule`/`/babysit` invocation), `remote` (tip 35, fires on first `RemoteTrigger`/`PushNotification`/`SendMessage` tool call), and `verification` (tip 73, fires on first `/go`). Supporting this, `scripts/_usage-data.mjs::scanTranscriptModes` gains a per-session `commands: Set<string>` field (purely additive) used by the two transcript-required detectors. The probe-tracker spec is updated in-sync with a new Part 1 Progression layer (3 rows) and Part 2 evidence entries for tips 35/48/73. The change brings detector coverage from 8 of 12 scored dimensions to 11 of 12, with backdated timestamps confirmed live.
- PR #110: Introduces a POSTURE_COMMANDS / VOLUME_COMMANDS partition in `scripts/_usage-data.mjs` to fix observer-session false positives in transcript-based command counting. Posture commands (`/color`, `/voice`, `/focus`, `/btw`, `/clear`, `/compact`, `/simplify`, `/rewind`, `/fewer-permission-prompts`) are now only counted when `classifySessionKind` returns `interactive_cli` or `"unknown"`, preventing observer and SDK-orchestrated sessions from inflating those counters by echoing the primary session's markup. Volume commands (`/loop`, `/schedule`, `/babysit`, `/go`, `/batch`) continue to be counted across all session kinds so that autonomous-workflow signal is preserved. A fail-loud `assertCommandPartition` guard is added at module load to catch future drift. Eleven new tests cover the helper and fixture-level behavior. The design spec and implementation plan are committed under `docs/superpowers/specs/` and `docs/superpowers/plans/` respectively, and the probe implementation status tracker is updated with a footnote on the Transcripts layer.
- PR #111: Migrated the docs-agent nightly GitHub Actions workflow from the deprecated `app-id` input to `client-id` on `actions/create-github-app-token@v3`, and moved `JIRA_EMAIL` from repository secrets to repository variables. Both changes are two-line edits to `.github/workflows/docs-agent-nightly.yml`.
- PR #113: PR #113 (CCE-72) fixes a multi-generation ship-journal parsing gap in the scoring engine. The existing `gatherShipJournal` function only detected stage execution via the oldest singular `entry.stage` field, silently missing ~41% of journal entries that use the newer `stages_run` array format (both integer and string variants). A new pure helper `stageRanInEntry(entry, legacyNumber, newName)` collapses all three format-generation detections into a single strict-equality Array.includes check. `gatherShipJournal` now tracks both a widened `stage2Count` (verify-agent, Stage 2) and a new `simplifyStageCount` (Stage 3). The `simplifyCommandUses` projection in `run-assessment.mjs` MAX-merges the new journal counter, mirroring the existing `/color` MAX-merge pattern. The journal lookback also widens from 14 → `insightsLookbackDays` (default 30) to align with transcript-derived signals. Thirteen new unit tests cover all three format generations plus edge cases. The probe-implementation-status tracker and CLAUDE.md conventions are updated in the same PR.
- PR #114: Four shipped engineering-plan documents were moved from `docs/superpowers/plans/` into `docs/superpowers/plans/archived/`. The plans covered: runtime-adoption-probes (#94), CCE-33 progression-detectors (#108), per-command-partition (#110), and predicate-ranker (#104). Pure rename operation — no content was altered.
- PR #116: PR #116 (CCE-76) activates real Execution scorers for two previously unmeasured dimensions — Memory & Context Management and Terminal & Customization — by replacing the `noTelemetry()` stubs with `withGates({ transcripts: true, universe: "interactive_or_unknown" })` ratio scorers. It unifies `focusCommandUses` and `rewindCommandUses` from per-message to per-session-coverage counting (matching the canonical pattern for `/btw`, `/clear`, `/compact`), adds an `interactiveOrUnknownSessionsAnalyzed` denominator signal (interactive_cli ∪ unknown sessions), and introduces a new `interactive_or_unknown` universe option in `withGates` to satisfy the numerator-subset-of-denominator hard rule. The Execution overall drops from 77 → 66 as a correct consequence of including the two newly measured dimensions (Memory: ex 16, Customization: ex 3) in the average. The probe tracker, methodology page, and CLAUDE.md scoring-model paragraph are updated to reflect all 12 dimensions now being measured.
- PR #117: Release v0.9.18 bundles 13 PRs landed since v0.9.17. The headline changes are: (1) Memory and Terminal & Customization Execution scorers replaced their noTelemetry() stubs with transcript-gated ratio scorers, meaning all 12 dimensions are now Execution-scored; (2) a per-command POSTURE/VOLUME partition prevents observer and SDK sessions from inflating posture-command counters, with a fail-loud boundary assertion at module load; (3) /ship Stage 2 (verify-agent) and Stage 3 (simplify) adoption counters now correctly traverse all three journal format generations; (4) the predicate evaluator was canonicalized to scripts/predicate.mjs with a passthrough re-export in TypeScript, and pre-computed rankedNextActions land in assessment.json; (5) the /progression timeline gained telemetry-dated detectors for the previously uncovered scheduled, remote, and verification dimensions. Additional changes include onboarding the engineering-docs-agent plugin, fixing a state.json shape bug, and migrating the Jira workflow to client-id auth.
- PR #118: Two completed plan documents — for CCE-72 (/ship Stage 2/3 journal-credit) and CCE-76 (Memory + Customization Execution scorers) — were mechanically moved from the active plans directory into `docs/superpowers/plans/archived/` via `git mv`. No content was changed; this is a pure housekeeping archival step following shipment of both features in v0.9.18 (PRs #113 and #116).
- PR #119: Removed the Math.max blend that was conflating cliBtwUseCount (cumulative all-time from ~/.claude.json) into btwCommandUses (30-day windowed session-coverage) inside signalsSummary. A new separate field cliBtwUseCountAllTime now exposes the cumulative source independently. The btw-side-channel predicate (Boris tips 33 and 54) was rerouted from btwCommandUses>=1 to cliBtwUseCountAllTime>=1, correctly matching the 'have you ever adopted this habit' semantic. Three tests were added, probe and signalsSummary tracker counts were updated (probes 47→48, signalsSummary keys 71→72), and a CLAUDE.md hard rule was added codifying the cumulative-vs-windowed counter partition for future scorer authors. Memory Execution score is unchanged by design (was 16, stays 16) because the scorer body already used maxProbe directly rather than the corrupted summary field.
- PR #120: Cuts v0.9.19, a scoring-honesty bugfix release bundling two earlier PRs. PR #119 (CCE-78) removes the Math.max blend of the cumulative all-time `~/.claude.json#btwUseCount` counter into the 30-day windowed `signalsSummary.btwCommandUses` field. The cumulative count is now exposed as a separate `cliBtwUseCountAllTime` field, and the tip-33/54 `btw-side-channel` rubric predicate is rerouted to the new field, preserving predicate correctness while restoring honest windowed semantics on `btwCommandUses`. A new CLAUDE.md hard rule documents the cumulative-vs-windowed counter contract for future scorer authors. PR #118 archives the completed CCE-72 and CCE-76 plans from `docs/superpowers/plans/`.
- PR #121: Upgraded the engineering-docs-agent integration from `framework: none` to `framework: mkdocs`, standing up a published Material-theme docs site at https://theoju.github.io/claude-code-self-assessment/. Includes a new `mkdocs.yml` config (search, awesome-pages, literate-nav plugins), `requirements-docs.txt` with 5 pinned dependencies, migration of existing flat `docs/*.md` files into `docs/site-src/` with 9 broken relative links repaired (cross-tree refs rewritten to absolute GitHub blob URLs), two new CI workflows (`docs-agent-pages.yml` for push-to-main deploy and `docs-build-check.yml` for PR-level `mkdocs build --strict` gate), a config flip in `.engineering-docs-agent/config.yml` activating the publish-verifier stage of the nightly, path-reference updates across 6 files (README.md, CLAUDE.md, rubric.json, ship-pattern page, slash commands, skill), and 3 new vitest files covering path-migration correctness, scaffold existence/content, and config contract (21 new test cases, 689 total passing).
- PR #122: Post-merge follow-up to PR #121 / CCE-81 recording three durable lessons from the actual GitHub Pages deploy incident: (1) `actions/configure-pages@v6 enablement: true` does NOT bootstrap Pages on first deploy — the workflow GITHUB_TOKEN lacks admin scope and `permissions:` blocks can only restrict default scopes, never expand them; the fix is a one-time `gh api -X POST repos/<owner>/<repo>/pages -f build_type=workflow`; (2) monitor scripts must avoid assigning to zsh read-only built-ins (`status`, `pipestatus`) or use a `#!/usr/bin/env bash` shebang, since hitting either crashes the shell silently; (3) a POST-IMPLEMENTATION CORRECTION block was appended to the mkdocs-upgrade spec and a 'Post-merge outcomes' section to the plan documenting all three deviations from the planned rollout (Pages failure + recovery, CCE-81 filed post-execution, monitor crashes under zsh).
- PR #125: Dropped the misleading `enablement: true` field from the `docs-agent-pages.yml` GitHub Actions workflow (it was a no-op because GITHUB_TOKEN lacks the admin scope required to bootstrap Pages) and cleaned the associated comment block. Flipped the matching vitest assertion from a positive match to a regression guard that rejects any re-introduction of the field. Appended a one-line resolution footer to the existing POST-IMPLEMENTATION CORRECTION block in the mkdocs upgrade design spec. Shortened the CLAUDE.md Pages-enablement gotcha bullet to point at the engineering-docs-agent plugin's CLAUDE.md as the durable authoritative source rather than repeating the full explanation inline.
- PR #127: Added a new conventions bullet to CLAUDE.md requiring that plan-step verification use the actual consumer tool (e.g., `mkdocs build --strict`, `npx tsc --noEmit`) rather than bare filesystem checks (`test -f`). The rule is byte-identical across three sibling repositories (ADIS, engineering-docs-agent, and this repo), verified by MD5 and three-way diff.

