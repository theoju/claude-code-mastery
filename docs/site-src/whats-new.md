# What's New

The engineering-docs-agent appends entries here on each nightly run
when it detects merged work worth surfacing in a user-facing
changelog. Most entries are written by the agent and reviewed via
the `docs-agent/YYYY-MM-DD` PR.

<!-- The first authored entry will land below on the first nightly
     after the mkdocs upgrade ships. -->
## 2026-06-22T09:14:03.821118+00:00
- PR #101: PR #101 adds a new Conventions entry to CLAUDE.md documenting the `/progression` timeline's dual milestone-source architecture: telemetry detectors in `scripts/progression.mjs` self-date from session `start_time` over full history, while config detectors in `scripts/config-progression.mjs` stamp `firstSeenAt` at first-observed run and freeze it. It records the deliberate first-run caveat that causes all 8 config milestones to share the identical `2026-05-09T08:37:16.111Z` timestamp. It also documents the coverage gap — 3 of 12 scored dimensions (`scheduled`, `remote`, `verification`) have no milestone detector, tracked as CCE-33. Additionally fixes two file-map entries in CLAUDE.md: adds the missing `config-progression.mjs` entry and corrects the stale comment on `app/progression/page.tsx`.
- PR #100: PR #100 onboards the claude-code-self-assessment repository onto the engineering-docs-agent nightly pipeline. It adds the plugin host configuration (`.engineering-docs-agent/config.yml`) using `framework: none` (enabled by CCE-64), seeds the agent state file, provides a state template, and wires up a `.github/workflows/docs-agent-nightly.yml` workflow incorporating recent plugin fixes (CCE-45 App-token wiring, CCE-49 OAuth validation, CCE-41 forensics upload, CCE-53 Jira wiring). A previously added synthetic mkdocs scaffold was removed once `framework: none` became a first-class option, so the repo now accurately reflects its plain-markdown/GitHub-rendered shape with no SSG build step.
- PR #102: Fixed a schema mismatch in `.engineering-docs-agent/state.json` by changing `dismissed_gap_flags` from an empty array `[]` to an empty object `{}`. The plugin's state schema requires this field to be an object keyed by `{owner}/{name}#{pr}`, but the bootstrap PR had seeded it incorrectly as an array. The orchestrator's strict JSON schema validation was failing immediately (exit code 2, within ~0.2 s of startup) before any subagent could be dispatched, completely blocking the docs-agent nightly workflow.
- PR #104: PR #104 adds a tactical fix for the recurring bug where the `/self-assessment` skill's model re-implements the `satisfiedWhen` DSL filter from scratch and gets it wrong. Specifically, it inserts a 12-line DSL grammar reference block into `.claude/skills/self-assessment/SKILL.md` documenting all 7 operator classes (with a canonical-implementation pointer to `app/lib/assessment.ts:evaluatePredicate`) so a model running the skill can evaluate predicates correctly without reimplementing them. The PR also ships the design spec (`docs/superpowers/specs/2026-05-31-predicate-ranker-design.md`) and implementation plan for the structural follow-up (PR 2), which will extract `evaluatePredicate` to `scripts/predicate.mjs` and pre-compute ranked next-actions into `assessment.json.rankedNextActions`, making the skill a trivial reader and rendering the grammar block obsolete.
- PR #106: Extracted a canonical predicate DSL evaluator into `scripts/predicate.mjs` (ported byte-faithfully from `app/lib/assessment.ts`) and collapsed the TypeScript copy to a 2-line re-export, enforced by a reference-identity CI test. Added `scripts/rank-next-actions.mjs` — a `rankNextActions()` function that uses the canonical evaluator to filter satisfied actions and ranks the remainder by weight × deficit with a deterministic 5-tier tie-break. Wired the ranker into `scripts/run-assessment.mjs` so every `npm run assess` writes a pre-computed `rankedNextActions[10]` array into `assessment.json`. Simplified the `/self-assessment` SKILL.md to read from that pre-computed array rather than re-implementing ranking logic. Added two CLAUDE.md hard rules locking down the single-source evaluator and the pre-computed next-actions contract. Pinned the originating production bug (babysit-loop surfacing as a priority despite loopCommandUses=14) as a named CI regression test.
- PR #107: Pure test hardening with no production code changes. Two deferred minor findings from the PR #106 predicate-ranker review cycle were closed: (1) an explicit regression test was pinned for the internal `axisOrder()` mapping to assert unknown axis values sort at tier 2 rather than ahead of platform, so any future enum-flip fails loudly; (2) the `=` equality predicate test was extended to cover string-typed LHS values (`{ x: "5" }`) in addition to the existing numeric case, locking the `String(value) === lit` cross-type-coercion contract. A stale `stash@{0}` WIP entry superseded by PRs #104 and #106 was also dropped.
- PR #108: PR #108 closes the `/progression` timeline coverage gap (CCE-33) by adding three telemetry-dated milestone detectors to `scripts/progression.mjs`: `scheduled` (Boris tip 48, fires on the first `/loop`, `/schedule`, or `/babysit` invocation), `remote` (tip 35, fires on the first `RemoteTrigger`, `PushNotification`, or `SendMessage` tool event), and `verification` (tip 73, fires on the first `/go` invocation). A supporting change extends `scanTranscriptModes` in `scripts/_usage-data.mjs` with a per-session `commands: Set<string>` field, enabling the two transcript-dependent detectors. The probe-tracker spec (`docs/superpowers/specs/2026-05-25-probe-implementation-status.md`) is synced in the same PR with a new Part 1 Progression layer and Part 2 evidence updates for tips 35, 48, and 73. Previously only 8 of 12 scored dimensions had progression detectors; this lands the missing three. No UI changes are required — the existing `/progression` page renderer consumes the new milestones through the uniform pipeline.
- PR #110: Introduced a per-command partition in the transcript-scanning layer (`scripts/_usage-data.mjs`) that separates posture commands (`/color`, `/voice`, `/focus`, `/btw`, `/clear`, `/compact`, `/simplify`, `/rewind`, `/fewer-permission-prompts`) from volume commands (`/loop`, `/schedule`, `/babysit`, `/go`, `/batch`). Posture commands are now counted only when `classifySessionKind` returns `interactive_cli` or `"unknown"`, preventing observer and SDK-orchestrated sessions from inflating posture counters by echoing the primary session's command markup. Volume commands continue to be counted across all session kinds. A fail-loud `assertCommandPartition` guard runs at module load to catch future drift (disjointness, missing classification, dead classification). Two thresholds crossed downward as a result (`simplifyCommandUses` and `rewindCommandUses`), which the spec identified as the correct behavior — their non-zero prior counts were entirely sourced from observer-session false positives.
- PR #111: Two-line edit to the docs-agent nightly GitHub Actions workflow: the deprecated `app-id` input for `actions/create-github-app-token@v3` was replaced with the current `client-id` input sourced from a repository variable (`vars.DOCS_AGENT_APP_CLIENT_ID`), and `JIRA_EMAIL` was migrated from repository secrets to repository variables because it is a basic-auth username rather than a credential.
- PR #113: Added a pure helper `stageRanInEntry(entry, legacyNumber, newName)` in `scripts/signals.mjs` to detect `/ship` stage execution across all three `~/.claude/ship/journal.jsonl` format generations (singular `entry.stage`, legacy-numeric `stages_run` array, and new-string `stages_run` array). Widened `gatherShipJournal` to count both Stage 2 (verify-agent) and a new Stage 3 (simplify) signal, fixing a gap where ~41% of journal entries were invisible to scoring. Updated `run-assessment.mjs` to MAX-merge the new `simplifyStageCount` into the `simplifyCommandUses` projection, mirroring the v0.9.16 `/color` pattern. Widened the production journal lookback from 14 to `insightsLookbackDays` (default 30) to align journal-derived signals with transcript-derived ones.
- PR #114: Four shipped engineering plan documents were moved from docs/superpowers/plans/ into docs/superpowers/plans/archived/. The plans cover runtime-adoption probes (PR #94), CCE-33 progression detectors (PR #108), per-command partition (PR #110), and the predicate ranker (PR #104). Pure file-move with no content changes; 100% rename similarity confirmed.
- PR #116: PR #116 (CCE-76) replaces `noTelemetry()` stubs for the Memory & Context Management and Terminal & Customization Execution scorers with real `withGates({ transcripts: true, universe: 'interactive_or_unknown' })` ratio scorers. It adds a new `interactive_or_unknown` session universe (interactive_cli ∪ unknown), a matching `interactiveOrUnknownSessionsAnalyzed` denominator signal in insights-signals, and unifies `focusCommandUses` / `rewindCommandUses` from per-message invocation counts to per-session-coverage flags — matching the canonical pattern already used for /btw, /clear, and /compact. The Execution composite drops from 77 → 66 because two previously-excluded low-scoring dimensions now contribute to the average. The methodology page, probe-tracker spec, and CLAUDE.md scoring-model paragraph are all updated to reflect that all 12 dimensions are now measured. 19 new tests were added (total suite: 647 → 666 passing).
- PR #117: Release v0.9.18 bundles 13 PRs landed since v0.9.17. The headline change is CCE-76 (PR #116): Memory and Terminal & Customization Execution scorers are now ratio-based, gated to the interactive_cli ∪ unknown session universe via MAX-merged transcript and history posture-command counters, completing Execution coverage across all 12 scored dimensions. CCE-71 (PR #110) introduced a fail-loud per-command POSTURE/VOLUME partition that prevents observer/SDK/subagent sessions from inflating posture counters for commands like /clear, /compact, and /btw. CCE-72 (PR #113) fixed an undercount of /ship Stage 2 (verify-agent) and Stage 3 (simplify) adoption signals across all three journal format generations. PR #106 established scripts/predicate.mjs as the single canonical DSL evaluator with app/lib/assessment.ts reduced to a 1-line passthrough, and introduced pre-computed rankedNextActions in assessment.json. CCE-33 (PR #108) added telemetry-dated detectors for the scheduled, remote, and verification dimensions on the /progression timeline, closing a coverage gap. Supporting changes include engineering-docs-agent plugin onboarding (CCE-57/PR #100), a state.json dismissed_gap_flags shape fix (CCE-65/PR #102), and Jira workflow migration to client-id auth (CCE-66/PR #111).
- PR #118: Two completed plan files — CCE-72 (/ship Stage 2/3 journal-credit) and CCE-76 (Memory + Customization Execution scorers) — were moved via `git mv` from the active plans directory into `docs/superpowers/plans/archived/`. No content was changed.
- PR #119: PR #119 fixes a data-integrity bug in the scoring layer where `signalsSummary.btwCommandUses` was incorrectly blended with `~/.claude.json#btwUseCount` (a cumulative all-time invocation count) via `Math.max`, conflating two independent semantic axes: (a) time window (30-day windowed vs. cumulative lifetime) and (b) counter class (per-session-coverage vs. raw invocation count). The fix removes the blend, exposes `cliBtwUseCountAllTime` as a separate cumulative field in `signalsSummary`, and reroutes the `btw-side-channel` rubric predicate (Boris tips 33 and 54) to the correct cumulative source. A hard rule documenting the two-axis classification requirement for any future ratio-numerator additions is added to CLAUDE.md. Memory Execution score is intentionally unchanged because the scorer body already used `maxProbe` directly and was not affected by the corrupted summary surface.
- PR #120: Cuts v0.9.19, bundling two earlier PRs: (1) the `signalsSummary.btwCommandUses` field no longer Math.max-blends the cumulative all-time `~/.claude.json#btwUseCount` counter with the 30-day windowed session-coverage value — the cumulative source is now exposed separately as `cliBtwUseCountAllTime`, and the rubric `btw-side-channel` predicate (tips 33 and 54) is rerouted to it; (2) completed plans for CCE-72 and CCE-76 are archived. A new CLAUDE.md hard rule codifies the cumulative-vs-windowed counter-blending prohibition for scorer authors going forward.
- PR #121: Upgrades the engineering-docs-agent integration from framework:none to framework:mkdocs, publishing a Material-theme MkDocs site at https://theoju.github.io/claude-code-self-assessment/. Adds two CI workflows — a push-to-main Pages deploy (docs-agent-pages.yml) and a PR-level mkdocs build --strict gate (docs-build-check.yml). Migrates existing flat docs/*.md files verbatim into docs/site-src/, repairs 9 broken relative links (cross-tree refs to .claude/ and docs/superpowers/ rewritten to absolute GitHub blob URLs), and updates path references across CLAUDE.md, README.md, rubric.json, and three skill/command files. Three new Vitest test files (21 cases) cover path migration, scaffold integrity, and config contract. The .engineering-docs-agent/config.yml framework field, whats_new_file, lens_paths.core, and publishing fields are all live.
- PR #122: Post-merge follow-up to PR #121 (CCE-81) recording three durable operational lessons from the GitHub Pages deploy incident: (1) `actions/configure-pages@v6 enablement: true` does NOT self-bootstrap Pages on a first deploy because the workflow GITHUB_TOKEN lacks admin scope — the fix is a one-time `gh api -X POST repos/.../pages -f build_type=workflow` call or Settings UI toggle; (2) monitor scripts targeting zsh must avoid assigning to the reserved built-ins `status` and `pipestatus` (crashes with 'read-only variable') — rename locals or shebang under bash; (3) a corollary that a monitor exiting non-zero with no event lines is almost always a script bug, not a failure of the watched system. Changes are docs-only: two new CLAUDE.md convention bullets, a POST-IMPLEMENTATION CORRECTION block added to the mkdocs upgrade spec under Gate 5, and a 'Post-merge outcomes' section appended to the upgrade plan documenting all three deviations from the planned rollout.
- PR #125: Removed the misleading `enablement: true` line from the `docs-agent-pages.yml` GitHub Actions workflow (it was a no-op because the workflow token lacks the admin scope to bootstrap Pages, and Pages was already bootstrapped manually during the CCE-81 incident). Flipped the matching vitest assertion to a regression guard that rejects re-adding the line. Appended a one-line resolution footer to the POST-IMPLEMENTATION CORRECTION block in the mkdocs upgrade design spec. Trimmed the CLAUDE.md Pages-bootstrap gotcha bullet to defer to the plugin's CLAUDE.md for the full root-cause explanation.
- PR #127: Added a new 'Plan-step verification' convention to CLAUDE.md mandating that plan authors invoke the actual consumer tool (e.g., `mkdocs build --strict`) rather than relying solely on filesystem existence checks (e.g., `test -f`) when verifying plan step outputs. The rule body is byte-identical across this repo, the advanced-data-import-system repo (PR #417, primary spec home), and the engineering-docs-agent repo (PR #106), verified via MD5 three-way diff by the orchestrator.

