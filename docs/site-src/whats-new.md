---
status: draft
sources:
  - https://github.com/theoju/claude-code-self-assessment/pull/121
synthesized_into: []
---

# What's New

The engineering-docs-agent appends entries here on each nightly run
when it detects merged work worth surfacing in a user-facing
changelog. Most entries are written by the agent and reviewed via
the `docs-agent/YYYY-MM-DD` PR.

## 2026-07-06T08:39:15.150012+00:00
- PR #101: Documents the /progression timeline's dual milestone-source architecture: telemetry detectors (scripts/progression.mjs) self-date from session start_time over full history, while config detectors (scripts/config-progression.mjs) stamp firstSeenAt at first-observed and freeze it, producing an identical first-run timestamp across all 8 config milestones. Also records a coverage gap where 3 of 12 scored dimensions (scheduled, remote, verification) have no milestone detector, and fixes a stale file-map comment plus a missing config-progression.mjs entry.
- PR #100: Bootstrapped the engineering-docs-agent plugin onto this repo: added `.engineering-docs-agent/config.yml` (single `core` lens rooted at `docs/`, `framework: none`, publishing fields nulled out since there's no GitHub Pages workflow yet), seeded `state.json` / `state.example.json`, and added a nightly GitHub Actions workflow that runs the docs-agent pipeline (source-collector, pr-summarizer, page-author, content-validator, gap-detector, notifier) without any mkdocs build/publish step. An earlier revision of this branch carried a synthetic `mkdocs.yml` + `requirements-docs.txt` scaffold to satisfy the plugin's old enum requiring `mkdocs` or `docusaurus`; once the plugin added `framework: none` as a first-class config value, that synthetic scaffold was removed so the config now accurately reflects that this repo renders plain markdown via GitHub with no static-site generator.
- PR #102: Fixed the engineering-docs-agent's committed state.json file: the `dismissed_gap_flags` field was seeded as an empty array (`[]`) instead of the schema-required empty object (`{}`), which caused the orchestrator to fail strict schema validation and exit with code 2 within ~0.2s of starting, before any subagent could dispatch.
- PR #104: Adds an interim grammar reference to the /self-assessment skill's SKILL.md documenting all 7 satisfiedWhen DSL operator classes (with a canonical-implementation pointer to app/lib/assessment.ts:evaluatePredicate and a worked example), and ships the accompanying design spec and implementation plan for a follow-up predicate-ranker structural fix. This is a tactical, docs-only patch: no scoring or runtime code changed.
- PR #106: Extracted the satisfiedWhen predicate DSL evaluator into a canonical scripts/predicate.mjs module (with app/lib/assessment.ts collapsed to a re-export, enforced by a reference-equality test), and added scripts/rank-next-actions.mjs which filters already-satisfied next-actions using that canonical evaluator and ranks the rest by weight × deficit with a deterministic tie-break. This is wired into scripts/run-assessment.mjs so every `npm run assess` run writes a pre-computed rankedNextActions[10] array into assessment.json, and the self-assessment skill now just reads that array instead of re-implementing the filter/ranking logic itself.
- PR #107: Added targeted regression tests closing two deferred Minor review findings from the predicate-ranker work (PR #106): a test pinning the axisOrder() tier mapping so an unknown axis value sorts adjacent to 'either' rather than ahead of 'platform', and an extension of the '=' equality test asserting evaluatePredicate("x=5", { x: "5" }) returns true to lock in the String(value) === lit cross-type-coercion contract. Also dropped a stale git stash entry superseded by what PR #104 + #106 actually shipped. No production code was touched.
- PR #108: Added three new telemetry-dated milestone detectors to the /progression timeline — 'scheduled' (fires on first /loop, /schedule, or /babysit usage), 'remote' (fires on first RemoteTrigger/PushNotification/SendMessage tool invocation), and 'verification' (fires on first /go usage). This closes a coverage gap where the progression milestone catalog covered only 8 of the 12 scored dimensions, leaving scheduled/remote/verification usage invisible on the timeline even with heavy real usage. The supporting change extends the transcript scanner to track a per-session set of invoked slash commands, which the two transcript-dependent detectors consume. The probe-implementation-status tracker spec was updated in the same PR per project convention, though no new predicates or catalog entries were added.
- PR #110: Transcript-based command counting now partitions commands into two disjoint sets: posture commands (/color, /voice, /focus, /btw, /clear, /compact, /simplify, /rewind, /fewer-permission-prompts) are only counted when a session classifies as interactive_cli or unknown, while volume commands (/loop, /schedule, /babysit, /go, /batch) continue to be counted across all session kinds. A fail-loud assertCommandPartition check runs at module load to catch any future drift (disjointness, missing classification, or dead classification) between the two sets.
- PR #111: The nightly docs-agent GitHub Actions workflow (`docs-agent-nightly.yml`) was updated to migrate its GitHub App token authentication from the deprecated `app-id` input (backed by a repo secret) to the newer `client-id` input (backed by a repo variable), and to move the `JIRA_EMAIL` value from a secret to a repo variable since it is a basic-auth username rather than a credential.
- PR #113: Widened the `/ship` Stage 2 (verify-agent) and Stage 3 (simplify) telemetry credit so it recognizes dispatches across all three historical `~/.claude/ship/journal.jsonl` format generations (singular `entry.stage`, legacy-numeric `stages_run`, new-string `stages_run`). A new pure helper, `stageRanInEntry(entry, legacyNumber, newName)`, centralizes the three-way detection with strict-equality `Array.includes` checks. `gatherShipJournal` now returns a widened `stage2Count` plus a new `simplifyStageCount`, which `run-assessment.mjs` MAX-merges into the `simplifyCommandUses` projection (mirroring the existing `/color` pattern). The journal lookback window at the production call site was also widened from a fixed 14 days to the configurable `insightsLookbackDays` (default 30) so journal-derived signals align with transcript-derived ones. The probe-implementation-status tracker and CLAUDE.md were updated to document the new pattern and its footnoted rows.
- PR #114: Moved four already-shipped planning documents from docs/superpowers/plans/ into docs/superpowers/plans/archived/. This is a pure file relocation (100% rename similarity) with no content edits, code changes, or behavior changes.
- PR #116: Replaced the placeholder `noTelemetry()` handling for the Memory & Context Management and Terminal & Customization Execution dimensions with real ratio-based scorers driven by transcript-derived posture-command counters. `focusCommandUses` and `rewindCommandUses` were unified from raw per-message invocation counts to session-coverage counts (matching the existing `/btw`, `/clear`, `/compact` pattern), a new `interactiveOrUnknownSessionsAnalyzed` denominator was added, and a new `interactive_or_unknown` universe option was introduced to `withGates` so the numerator stays a strict subset of the denominator. As a result, all twelve Execution dimensions are now numerically measured; the two previously-unmeasured dimensions surface real (low) scores, which correctly pulls the aggregate Execution score down since it now reflects genuine low usage instead of excluding it.
- PR #117: Version bump to 0.9.18 with no direct code changes (only package.json updated). The release bundles 13 previously-merged PRs since v0.9.17: Memory & Customization Execution scorers replacing noTelemetry() placeholders (completing Execution scoring for all 12 dimensions), a posture/volume command-counting partition that prevents observer/SDK sessions from inflating posture counters, a fix to ship-journal stage counting across all three journal format generations, a canonical predicate evaluator with pre-computed ranked next-actions in assessment.json, new telemetry-dated progression detectors for scheduled/remote/verification dimensions, plus assorted fixes (dismissed_gap_flags shape, Jira workflow auth migration, plan archival, docs refresh).
- PR #118: Mechanical archival of two already-shipped internal plan documents (CCE-72 ship-journal stage credit, CCE-76 Memory + Customization Execution scorers) via `git mv` into `docs/superpowers/plans/archived/`. No content was altered.
- PR #119: Fixed a scoring-surface bug where `signalsSummary.btwCommandUses` was Math.max-blending a cumulative all-time counter (`~/.claude.json#btwUseCount`) into a 30-day windowed session-coverage metric, conflating two independent semantic axes (time window and counter class). The fix exposes the cumulative source as a new separate field, `cliBtwUseCountAllTime`, and reroutes the `btw-side-channel` rubric predicate (Boris tip 33+54) to key off that cumulative field instead of the windowed one. The Memory Execution score itself is unchanged (16) since the scorer body already used `maxProbe()` directly and never consumed the blended summary field — only the `signalsSummary` reporting surface was corrupted and is now corrected.
- PR #120: Version bump to v0.9.19, bundling two prior PRs: the /btw scoring-honesty fix (unblending cumulative all-time /btw usage from the 30-day windowed signalsSummary.btwCommandUses field, and exposing the cumulative count separately as cliBtwUseCountAllTime) and a housekeeping archive of landed plans.
- PR #121: Upgraded the engineering-docs-agent integration for this repo from `framework: none` to `framework: mkdocs`, standing up a published Material-theme documentation site at https://theoju.github.io/claude-code-self-assessment/. The change adds an mkdocs scaffold (mkdocs.yml, pinned requirements-docs.txt, docs/site-src/ stubs), two new CI workflows (a push-to-main deploy workflow and a PR-level `mkdocs build --strict` gate), migrates the four existing flat docs/*.md files (plus images) into docs/site-src/ with repaired cross-tree links, flips five fields in `.engineering-docs-agent/config.yml` to activate the nightly's publish-verifier stage, and updates path references across README, CLAUDE.md, the rubric, the in-app ship-pattern page, and the self-assessment command/skill.
- PR #122: Adds three durable operational lessons discovered while recovering from a broken GitHub Pages deploy after PR #121 (the mkdocs upgrade): (1) `actions/configure-pages@v6`'s `enablement: true` does not actually bootstrap Pages on a first deploy because the workflow's default `GITHUB_TOKEN` lacks admin scope, and the real fix is a one-time `gh api -X POST .../pages -f build_type=workflow` call (or the equivalent Settings UI toggle); (2) monitor scripts must avoid assigning to zsh's read-only `status`/`pipestatus` built-ins, which crash the shell and silently mask successful deploys; and (3) a monitor exiting non-zero with no emitted events is almost always a script bug, not evidence the watched system failed. These are recorded as new CLAUDE.md Conventions bullets, a post-implementation correction block in the mkdocs-upgrade design spec, and a new 'Post-merge outcomes' section in the mkdocs-upgrade plan.
- PR #125: Dropped the misleading `enablement: true` line from this repo's `docs-agent-pages.yml` GitHub Actions workflow (and the matching wrong claim in its comment block), since it was a no-op that didn't actually bootstrap GitHub Pages. Flipped the corresponding vitest assertion into a regression guard against re-adding the line, appended a one-line 'Resolved by' footer to the existing POST-IMPLEMENTATION CORRECTION block in the mkdocs-upgrade spec, and shortened the related CLAUDE.md gotcha bullet to point at the plugin's CLAUDE.md as the durable source of the explanation.
- PR #127: Added a new project convention to CLAUDE.md: plan-step verification must invoke the actual consumer tool (e.g. mkdocs build --strict, tsc --noEmit) rather than relying on filesystem checks like `test -f`. The rule text is byte-identical to the same convention landed in two sibling repos (advanced-data-import-system and engineering-docs-agent).
- PR #128: Redesigned the Memory Execution scorer's ratio: the numerator is narrowed to only the two session-coverage-class posture commands (/clear + /compact), dropping /btw (a cumulative all-time counter) and /rewind (a near-zero binary signal) out of the ratio. /btw usage is now surfaced separately as cumulative evidence text via a new signalsSummary.cliBtwUseCountAllTime field, and /rewind remains only as a next-action probe. The rubric target for the memory dimension was recalibrated from 92 to 60 to match the narrower, more realistic ceiling. The methodology page narrative, test fixtures, and 11 test cases were updated to match, and a new CLAUDE.md hard rule codifies a two-axis (time-window x counter-class) categorization check for any future numerator field.
### Gaps flagged
- theoju/claude-code-self-assessment#100: No dismissed flag, no allowlist match, and size_filter is unconfigured (empty thresholds) so falls through to LLM judgment. The PR bootstraps a new automated subsystem into the repo: a nightly CI workflow wiring GitHub App-token auth, Jira transitions, and forensics upload, plus host-side plugin config that governs publishing behavior. Even though the underlying framework decision (framework=none) was specified upstream in CCE-64, the host-side integration itself (new automated pipeline touching CI, auth tokens, and Jira) is the kind of security/infra-relevant subsystem change a senior engineer would expect a short design/runbook doc for, distinct from a simple refactor or dependency bump.
- theoju/claude-code-self-assessment#106: No allowlist match and size_filter thresholds are unset, so the middle-tier LLM judgment applies. This PR introduces a new canonical subsystem (predicate.mjs DSL evaluator) and a new pre-computed ranking pipeline (rank-next-actions.mjs) wired into the assessment output contract, changing behavior consumed by the self-assessment skill and assessment.json schema — exactly the class of change (new subsystem, behavior-affecting contract change) a senior engineer would expect a spec/plan for. The PR body confirms this expectation was already met: it references a design doc (docs/superpowers/specs/2026-05-31-predicate-ranker-design.md) and plan (docs/superpowers/plans/2026-05-31-predicate-ranker.md).
- theoju/claude-code-self-assessment#108: No allowlist match and size_filter thresholds are unset, so this falls to LLM judgment. The PR adds new telemetry-dated milestone detectors (scheduled/remote/verification) to scripts/progression.mjs and extends scanTranscriptModes' per-session shape in _usage-data.mjs — a new subsystem behavior affecting the scored progression timeline, not a refactor or dependency bump. A senior engineer would expect a written spec/plan for this class of change, which is consistent with the PR itself shipping a design spec and implementation plan alongside the code.
- theoju/claude-code-self-assessment#110: Config has no allowlist match and size_filter thresholds are unset (files=6, touches core scoring logic in scripts/_usage-data.mjs), so this falls to LLM judgment. The change alters how transcript-derived posture commands are classified/counted (session-kind-gated partition) and introduces a new fail-loud invariant (assertCommandPartition) that affects Execution scoring output for a live dimension. This is a behavior-changing correctness fix to a measurement subsystem with cross-cutting effects on multiple counters/dimensions — exactly the class of change a senior engineer would expect a written spec/plan for, which this PR in fact already references.
- theoju/claude-code-self-assessment#113: Not in dismissed_flags. Allowlist is empty so no path match at that tier. size_filter has no min_loc/min_files thresholds configured, so the size-filter tier cannot be evaluated and falls through to LLM judgment. The PR changes core scoring/telemetry logic (widening ship-journal stage detection across three format generations, altering simplifyCommandUses and shipVerifyStageRecent projections, widening the lookback window) — a behavior change affecting predicate satisfaction and ranked next-actions, which is exactly the class of change (scoring-engine semantics change, cross-format compatibility logic) a senior engineer would expect a written spec/plan for. This is corroborated by the PR itself including a validated design spec and implementation plan.
- theoju/claude-code-self-assessment#116: No allowlist_paths configured and size_filter has no min_loc/min_files thresholds set, so this falls to LLM judgment. The PR introduces two new Execution scorers (memory, customization), a new universe option in withGates, a new denominator signal, and changes user-visible dashboard output (Execution composite score drops 77→66, new next-actions surface). This is exactly the kind of change — new subsystem behavior + visible scoring semantics change — a senior engineer would expect a written spec/plan for, and indeed the PR body confirms one was produced and reviewed by three independent agents.
- theoju/claude-code-self-assessment#121: Not dismissed; allowlist_paths is empty so no path can match; size_filter has no min_loc/min_files thresholds defined, so the size filter cannot gate the decision — falls to LLM judgment. The PR stands up a new subsystem (published mkdocs/GitHub Pages docs site), adds two new CI workflows, flips a config framework setting that activates a new nightly stage, and migrates/rewrites cross-tree links — a clear case where a senior engineer would expect a written spec/plan, which is consistent with the PR itself citing a spec and plan doc.

## 2026-07-06 — Docs site rebuilt on mkdocs (CCE-81)

The published docs site at
[theoju.github.io/claude-code-self-assessment](https://theoju.github.io/claude-code-self-assessment/)
now builds from a real mkdocs-Material scaffold instead of the prior
`framework: none` passthrough. PR #121 flips five fields in
`.engineering-docs-agent/config.yml` — `docs.framework`, `whats_new_file`,
`lens_paths.core`, and the `publishing.base_url` /
`publishing.build_workflow` pair — to activate the nightly's
publish-verifier stage against the new site.

What actually changed on disk:

- A new `mkdocs.yml` (Material theme, `navigation.tabs` /
  `navigation.sections`, `literate-nav` reading `docs/site-src/SUMMARY.md`
  for the nav tree) plus a pinned `requirements-docs.txt`.
- The four previously-flat `docs/*.md` pages — Self-Assessment, Ship
  Pattern, Boris Tips Reference, and Tip Classification — moved under
  `docs/site-src/`, along with their images, with cross-tree links
  repaired to match the new layout.
- Two CI workflows: `docs-agent-pages.yml` deploys the built site on
  every push to `main` that touches `docs/site-src/**`, `mkdocs.yml`, or
  `requirements-docs.txt`; `docs-build-check.yml` runs `mkdocs build --strict`
  as a PR gate so a broken internal link fails CI instead of the
  published site.
- Path references updated across `README.md`, `CLAUDE.md`,
  `app/data/rubric.json`, the in-app `/docs/ship-pattern` page, and the
  `self-assessment` command/skill, so nothing still points at the old
  flat `docs/` locations.

This was the upgrade path `.engineering-docs-agent/config.yml` had
documented as intended ever since `framework: none` became first-class
(CCE-64) — CCE-81 is the PR that executes it, after a spec, a plan, and
a 3-agent pre-execution validation pass that caught and fixed real
blockers (broken `.claude/`-relative links, missed path references, and
a missing PR-level build gate) before merge.

Two items from the PR body are still open and worth confirming before
treating this migration as fully closed out: that GitHub Pages
auto-deploy actually fires from `docs-agent-pages.yml`, and that the
next scheduled nightly run completes cleanly against
`framework: mkdocs`. Design and plan detail live in
`docs/superpowers/specs/2026-06-01-mkdocs-upgrade-design.md` and
`docs/superpowers/plans/2026-06-01-mkdocs-upgrade.md` in the source
tree (not published as lens pages).
