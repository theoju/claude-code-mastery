# What's New

The engineering-docs-agent appends entries here on each nightly run
when it detects merged work worth surfacing in a user-facing
changelog. Most entries are written by the agent and reviewed via
the `docs-agent/YYYY-MM-DD` PR.

<!-- The first authored entry will land below on the first nightly
     after the mkdocs upgrade ships. -->
## 2026-08-08T07:55:59.069649+00:00
- PR #101: Documents the /progression timeline's dual milestone-source architecture in the project's CLAUDE.md: telemetry milestones (scripts/progression.mjs) self-date from session start_time over full history independent of the insights lookback window, while config milestones (scripts/config-progression.mjs) stamp firstSeenAt at first-observed and freeze it — explaining why all 8 config milestones share an identical first-run timestamp. It also records a coverage gap where 3 of 12 scored dimensions (scheduled, remote, verification) have no milestone detector, so real adoption there never appears on the timeline, and fixes a stale file-map comment plus a missing config-progression.mjs entry.
- PR #100: Bootstraps the engineering-docs-agent plugin onto this repo by adding `.engineering-docs-agent/config.yml` (a single `core` lens rooted at `docs/`, publishing fields nullified), state-tracking files (`state.json`, `state.example.json`), and a nightly GitHub Actions workflow (`docs-agent-nightly.yml`) that runs the agent automatically. The config declares `framework: none` since this repo's docs are plain markdown rendered by GitHub with no static-site generator.
- PR #102: Fixed a JSON schema violation in `.engineering-docs-agent/state.json`: the `dismissed_gap_flags` field was seeded as an empty array (`[]`) instead of the empty object (`{}`) the plugin's state schema requires (it's keyed by `{owner}/{name}#{pr}`). This one-character fix was blocking the docs-agent nightly orchestrator, which exited with code 2 within ~0.2s of starting due to strict schema validation, before any subagent could dispatch.
- PR #104: Documented the `satisfiedWhen` predicate DSL grammar directly in the `/self-assessment` skill (`SKILL.md`), covering all 7 operator classes with a canonical-implementation pointer to `app/lib/assessment.ts:evaluatePredicate` and a worked example. Also adds a companion design spec and implementation plan describing the predicate-ranker fix this grammar block supports.
- PR #106: Extracted the satisfiedWhen predicate string-DSL evaluator into a single canonical module (scripts/predicate.mjs), with app/lib/assessment.ts's evaluatePredicate collapsed to a thin re-export rather than a duplicate implementation. rank-next-actions.mjs now uses this canonical evaluator to pre-compute a filtered, weight×deficit-ranked top-N next-actions list that gets written into assessment.json as rankedNextActions, so downstream consumers (including the /self-assessment skill) read pre-computed results instead of re-implementing the filter/ranking logic themselves.
- PR #107: Added two regression tests to the predicate-ranker test suite: one pinning the internal axisOrder() tie-break mapping so an unknown axis value sorts into tier 2 (adjacent to "either") rather than ahead of "platform", and one extending the "=" equality predicate test to assert string-vs-string equality (`evaluatePredicate("x=5", { x: "5" })` returns true).
- PR #108: Adds three new telemetry-dated milestone detectors — scheduled (fires on first /loop, /schedule, or /babysit), remote (fires on first RemoteTrigger/PushNotification/SendMessage tool call), and verification (fires on first /go) — to the /progression timeline's milestone catalog in scripts/progression.mjs, closing the coverage gap that previously left 4 of 12 scored dimensions (scheduled, remote, verification, and their transcript-derived siblings) undetected. Also extends _usage-data.mjs's scanTranscriptModes() with an additive per-session commands Set field that the two transcript-based detectors consume, and updates the probe-implementation-status tracker per the project's living-doc convention.
- PR #110: Introduces a fail-loud, per-command partition between 'posture' commands (/color, /voice, /focus, /btw, /clear, /compact, /simplify, /rewind, /fewer-permission-prompts) and 'volume' commands (/loop, /schedule, /babysit, /go, /batch) inside the transcript scanner. Posture commands are now only counted from transcripts when classifySessionKind returns interactive_cli or unknown, preventing observer and SDK-orchestrated sessions from inflating posture counters by echoing the primary session's <command-name> markup. Volume commands remain counted across every session kind since autonomous-workflow signal is valid regardless of session kind. A new assertCommandPartition check runs at module load to catch drift (disjointness, missing classification, dead classification) between the two command sets.
- PR #111: Updated the `.github/workflows/docs-agent-nightly.yml` GitHub Actions workflow to authenticate via `client-id: ${{ vars.DOCS_AGENT_APP_CLIENT_ID }}` instead of the deprecated `app-id: ${{ secrets.DOCS_AGENT_APP_ID }}` input on `actions/create-github-app-token@v3`, and switched `JIRA_EMAIL` from a repo Secret to a repo Variable since it's a basic-auth username rather than a credential.
- PR #113: Widens `/ship` Stage 2 (verify-agent) and Stage 3 (simplify) credit detection so that `gatherShipJournal` recognizes journal entries across all three `~/.claude/ship/journal.jsonl` format generations (singular `entry.stage`, legacy-numeric `stages_run`, and new-string `stages_run`) via a new pure helper `stageRanInEntry(entry, legacyNumber, newName)`. `gatherShipJournal` now widens `stage2Count` and adds a new `simplifyStageCount`, and `run-assessment.mjs` MAX-merges the simplify journal counter into the `simplifyCommandUses` projection, mirroring the existing v0.9.16 `/color` pattern.
- PR #114: Archived four already-shipped planning documents (runtime adoption probes, CCE-33 progression detectors, per-command partition, predicate ranker) by moving them from docs/superpowers/plans/ into docs/superpowers/plans/archived/. Pure file renames with no content changes.
- PR #116: Replaced the placeholder noTelemetry() scorers for the Memory and Terminal/Customization Execution dimensions with real ratio scorers (withGates({transcripts:true, universe:"interactive_or_unknown"})) that consume MAX-merged transcript+history posture-command counters. focusCommandUses and rewindCommandUses were unified from total-invocation counting to session-coverage counting, matching the existing pattern used for /btw, /clear, and /compact. A new interactiveOrUnknownSessionsAnalyzed denominator and interactive_or_unknown universe option were added so the numerator stays a strict subset of the denominator's session universe. This completes measured Execution scoring for all twelve rubric dimensions.
- PR #117: Bumped package.json to version 0.9.18. No source code changes in this PR itself; it is a release-tagging commit that bundles 13 previously-merged PRs since v0.9.17, headlined by Memory + Customization Execution scorers (CCE-76/PR #116) completing Execution-axis coverage for all 12 rubric dimensions, and a posture/volume command-counting partition fix (CCE-71/PR #110) that prevents observer/SDK sessions from inflating posture counters.
- PR #118: Mechanically moved two already-shipped plan documents (CCE-72 /ship Stage 2/3 credit across journal formats, and CCE-76 Memory + Customization Execution scorers) into the archived plans directory via git mv, with no content changes.
- PR #119: Fixed the Memory Execution scorer so that `signalsSummary.btwCommandUses` no longer blends the cumulative all-time `/btw` invocation count (`~/.claude.json#btwUseCount`) with the 30-day windowed transcript+history session-coverage count via `Math.max`. The cumulative count is now exposed separately as `cliBtwUseCountAllTime`, and the rubric's `btw-side-channel` predicate (Boris tip 33+54) is rerouted from `btwCommandUses>=1` to `cliBtwUseCountAllTime>=1`, which is the more accurate binary-adoption signal for that check.
- PR #120: Release-only change that bumps the package version to 0.9.19 (package.json), packaging two prior PRs (#119's fix to stop blending cumulative all-time /btw usage into the windowed Memory Execution signalsSummary field) plus general housekeeping. No new source behavior is introduced by this PR itself beyond the version bump.
- PR #121: Upgrades the engineering-docs-agent integration for this repo from `framework: none` to `framework: mkdocs`, standing up a published Material-theme documentation site on GitHub Pages. Adds a `mkdocs.yml` scaffold (search + awesome-pages + literate-nav, pinned via `requirements-docs.txt`), two CI workflows (`docs-agent-pages.yml` for push-to-main deploy, `docs-build-check.yml` for a PR-level `mkdocs build --strict` gate), and migrates the existing flat `docs/*.md` files verbatim into `docs/site-src/`.
- PR #122: Following the PR #121 / CCE-81 GitHub Pages deploy incident, this docs-only PR records three durable operational lessons that weren't captured in the original spec/plan: (1) `actions/configure-pages@v6`'s `enablement: true` does not actually bootstrap GitHub Pages on a first deploy because the workflow's GITHUB_TOKEN lacks admin scope, and the fix requires a one-time `gh api -X POST .../pages -f build_type=workflow` call or a manual Settings toggle; (2) once Pages is enabled via `build_type=workflow`, that setting also disables branch-deploy publishing, meaning `deploy-pages@v5`'s artifact upload becomes the only publish path; (3) a monitor-script lesson about zsh's read-only `status`/`pipestatus` parameters causing poll-loop crashes that silently mask successful deploys. The changes update `CLAUDE.md` Conventions plus the mkdocs-upgrade spec and plan docs with post-implementation correction notes.
- PR #125: Removed the misleading `enablement: true` line from the `docs-agent-pages.yml` GitHub Actions workflow (it never actually bootstrapped GitHub Pages — the workflow token lacks the admin scope required, so the field was a silent no-op once Pages already existed). Flipped the corresponding vitest assertion into a regression guard against re-adding the line, appended a one-line resolution footer to the existing POST-IMPLEMENTATION CORRECTION block in the mkdocs-upgrade design spec, and shortened the CLAUDE.md gotcha bullet to point at the plugin repo's CLAUDE.md as the durable source of the fix. Docs + workflow text only — no runtime behavior change.
- PR #127: Adds a new convention to CLAUDE.md requiring that plan-step verification use the actual consumer tool (e.g. `mkdocs build --strict`, `npx tsc --noEmit`, `ajv validate`) rather than a filesystem existence check (`test -f`) whenever a plan step produces a published artifact such as a docs-site link, TypeScript import, JSON Schema reference, or OpenAPI route. The rule text is byte-identical across three sibling repos (this repo, advanced-data-import-system, engineering-docs-agent) to keep planning conventions consistent org-wide.
- PR #128: Redesigned the Memory & Context Management Execution scorer's ratio numerator to include only semantically-consistent session-coverage signals (/clear + /compact); moved /btw usage to cumulative evidence text via a new signalsSummary field instead of blending it into the ratio; dropped /rewind from the ratio while retaining it as a next-action probe; and recalibrated the rubric target for this dimension from 92 to 60 to match the narrowed ceiling. Updated the scorer, rubric config, probe catalog, methodology page, and test fixtures/suite accordingly.
- PR #177: Rethemed the dashboard's global visual design from a warm/gold palette to a cool-graphite instrument-panel look with a single electric-blue accent color. The change was applied globally via the Tailwind v4 `@theme` token layer in `app/globals.css`, so all 8 pages and 6 shared components inherit the new palette from one file. Includes new shared primitive styles (`.card`, `.icon-btn`), blue focus rings, thin cool scrollbars, and a faint top-center accent glow. `PageNav.tsx` was also updated to match.
- PR #178: Adds two new entries to the repo's CLAUDE.md Conventions section documenting toolchain diagnosis learned during a Homebrew Intel→ARM migration: (1) a PATH gotcha where a stale `~/.zprofile` Homebrew shellenv call causes login/non-interactive shells to silently lose access to `node`/`npm`/`gh` while the interactive terminal still works, plus how to diagnose it with `zsh -lc`/`zsh -ic`/`zsh -c` and fix `.zprofile`; and (2) a note that `Cannot find module @rollup/rollup-darwin-arm64`-style vitest startup failures after a node reinstall are usually the npm optional-dependency bug, fixable with `npm ci` rather than a manual `rm -rf node_modules` + reinstall.
- PR #179: Corrected a stale test-count claim in CLAUDE.md (564 tests/39 files/~5s → 695 tests/48 files/~3s) and added two new Conventions entries codifying durable learnings from the 2026-07-30 session: (1) clarifying that the 'how many Boris tips?' question has three distinct correct answers (87 upstream corpus, 86 reference-doc items, 75 tracked set) rather than being a live contradiction, and (2) documenting two known /graphify structural artifacts (inflated community counts from uniform JSON records, and dangling cross-chunk edges silently dropped at build time) so future graph extractions aren't misread as architecture findings. A companion retrospective document for the session was also added.
### Gaps flagged
- theoju/claude-code-self-assessment#100: No allowlist paths configured and size_filter thresholds are unset, so the size-filter tier can't be evaluated definitively — falls to LLM judgment. The PR bootstraps an entirely new subsystem: onboarding the repo onto the engineering-docs-agent plugin, adding a host config (.engineering-docs-agent/config.yml), state tracking files, and a new nightly GitHub Actions workflow. This is exactly the class of change (new automated pipeline, new cross-repo integration surface, behavior that runs unattended on a schedule) a senior engineer would expect a spec/plan for, even though the PR body itself only narrates ad-hoc history (referencing CCE-64) rather than linking a formal spec/plan artifact.
- theoju/claude-code-self-assessment#106: Allowlist is empty (no match possible) and size_filter thresholds are unset in config, so no min_loc/min_files comparison is meaningful — falls to LLM judgment. The PR is a structural architecture change: it establishes scripts/predicate.mjs as the single canonical DSL evaluator (collapsing app/lib/assessment.ts's implementation to a passthrough) and introduces pre-computed rankedNextActions consumed by the self-assessment skill, closing out a real bug class (a hand-rolled ranker silently no-oping on the string DSL and surfacing an already-satisfied action as a top priority). This changes user-visible scoring/next-action behavior and sets a canonical-source contract multiple future PRs must respect — the kind of change a senior engineer would expect a written rationale for, even though this PR's own body suggests it was preceded by PR #104 as tactical groundwork.
- theoju/claude-code-self-assessment#108: New feature work: adds three new telemetry-dated milestone detectors (scheduled/remote/verification) to the progression system, extends a shared data-scanning utility (scanTranscriptModes) with a new field, and updates the probe-tracker per CLAUDE.md hard rules. This is new subsystem behavior (closing a documented coverage gap, CCE-33) rather than a refactor or dependency bump, so a senior engineer would expect a spec/plan — and indeed the PR itself includes both (docs/superpowers/specs/2026-06-01-cce-33-progression-detectors-design.md and docs/superpowers/plans/2026-05-31-cce-33-progression-detectors.md), confirming the expectation was met.
- theoju/claude-code-self-assessment#110: No allowlist paths configured, so tier 2 doesn't apply, and size_filter thresholds are unset so the size gate can't exclude it. On LLM judgment: this changes core scoring logic (posture-vs-volume command counting in scripts/_usage-data.mjs) with a fail-loud invariant check (assertCommandPartition) that affects user-visible dashboard metrics — a behavior change to a measurement subsystem, not a refactor or dependency bump. A senior engineer would expect a written spec/plan for this, and notably the PR itself already includes both (docs/superpowers/specs/2026-05-31-per-command-partition-design.md and docs/superpowers/plans/2026-05-31-per-command-partition.md), confirming the expectation was met in practice.
- theoju/claude-code-self-assessment#113: Not on allowlist (empty allowlist) and size_filter thresholds are unset in config so the size-filter tier cannot be applied definitively (per-file loc counts also absent from the PR object) — falls through to LLM judgment. This PR changes core scoring/telemetry logic (gatherShipJournal stage-detection, run-assessment.mjs journal-counter merge into simplifyCommandUses) affecting how the dashboard scores real users' /ship usage across three journal format generations — a behavior change to a measurement/scoring subsystem, not a refactor or dependency bump. Consistent with this repo's own convention (CLAUDE.md hard rules) of pairing scorer/signal changes with a spec+plan, and indeed the PR itself already ships docs/superpowers/specs/2026-06-01-ship-journal-stage-credit-design.md and the matching plan file, confirming a senior engineer would expect this class of change to be accompanied by a written spec/plan.
- theoju/claude-code-self-assessment#116: Empty allowlist and empty size_filter thresholds skip tiers 2-3 (no glob to match, no min_loc/min_files to compare against). Under LLM judgment: this PR adds new Execution scorers (memory, customization) with a new gating universe (`interactive_or_unknown`), changes counter semantics (total-invocation → session-coverage for focus/rewind commands), and touches core scoring logic (`score.mjs`, `insights-signals.mjs`, `_usage-data.mjs`) plus denominator/numerator subset invariants called out as a hard rule in CLAUDE.md. This is exactly the class of change (new subsystem behavior, ratio-scorer semantics change) a senior engineer would expect a written spec/plan for — and indeed the PR body cites one (docs/superpowers/specs/2026-06-01-memory-customization-execution-scorers-design.md, validated by three agents), consistent with expectation.
- theoju/claude-code-self-assessment#121: Allowlist is empty (no match) and size_filter thresholds (min_loc/min_files) are unset, so no tier-2/3 short-circuit applies. Falls to LLM judgment: this PR stands up a new published subsystem (Material-theme mkdocs site on GitHub Pages), adds two new CI workflows with a PR-level build gate, and migrates existing docs into a new directory structure — a new-subsystem, user-visible infrastructure change that a senior engineer would expect a spec/plan to precede. Notably the PR itself already includes both (docs/superpowers/specs/2026-06-01-mkdocs-upgrade-design.md and docs/superpowers/plans/2026-06-01-mkdocs-upgrade.md), consistent with that expectation.
- theoju/claude-code-self-assessment#128: No allowlist paths configured and size_filter thresholds are unset, so falls to LLm judgment. The PR redesigns the Memory Execution scorer's ratio numerator (removes /rewind, adds cumulative-evidence handling for /btw) and recalibrates a public rubric target (92→60) — a change in scored, user-visible output logic, matching the 'new subsystem / user-visible behavior change' category that expects a written spec/plan. Notably, the PR itself already includes a spec and plan doc under docs/superpowers/, confirming this class of change warrants one.

