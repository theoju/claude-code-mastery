---
status: draft
sources:
  - https://github.com/theoju/claude-code-self-assessment/pull/121
synthesized_into: []
---

# What's New

The engineering-docs-agent appends entries here on each nightly run
when it detects merged work worth surfacing in a user-facing
changelog. Most entries are written by the agent and reviewed via
the `docs-agent/YYYY-MM-DD` PR.

## 2026-08-16T07:40:06.563542+00:00
- PR #190: Advanced the engineering-docs-agent's collection bookmark in `.engineering-docs-agent/state.json` to commit 316cb69, ending a temporary hold that had kept the nightly PR-collection range pinned in place. Only the `last_successful_run`/bookmark values change; the state file's schema is unaffected.

## 2026-08-13T08:40:00.656336+00:00
- PR #101: Added a Conventions entry to CLAUDE.md documenting the /progression timeline's dual milestone-source architecture: telemetry milestones (scripts/progression.mjs) self-dated from session start_time over full history, and config milestones (scripts/config-progression.mjs) which have no embedded timestamp and are instead frozen at the firstSeenAt of the first assessment run. It also explicitly records the known coverage gap — scheduled, remote, and verification dimensions have no milestone detector yet — tracked as CCE-33.
- PR #100: Bootstraps the claude-code-self-assessment repository onto the engineering-docs-agent plugin, adding host configuration (.engineering-docs-agent/config.yml), a state seed file, and a nightly GitHub Actions workflow that invokes the docs agent. The plugin is configured with framework: none, so generated docs render as plain markdown via GitHub rather than through a static site generator.
- PR #102: Fixed a one-character JSON schema bug in the engineering-docs-agent's internal state file: `dismissed_gap_flags` was seeded as an empty array (`[]`) by the bootstrap PR but the plugin's state schema requires it to be an object (`{}`), causing the orchestrator to exit with code 2 on schema validation and blocking the docs-agent nightly run.
- PR #104: Adds a 12-line grammar reference block to the /self-assessment SKILL.md documenting all 7 `satisfiedWhen` operator classes, with an explicit pointer to `scripts/predicate.mjs` as the canonical DSL implementation. Also lands the design spec and plan for a follow-up predicate-ranker feature (PR 2 of 2, not yet implemented).
- PR #106: Extracts a canonical predicate/DSL evaluator into scripts/predicate.mjs (with app/lib/assessment.ts reduced to a 1-line passthrough re-export) and adds scripts/rank-next-actions.mjs, wired into run-assessment.mjs so every `npm run assess` run pre-computes and writes a top-10 `rankedNextActions` array into assessment.json. The self-assessment skill now reads that pre-computed ranking instead of re-implementing the satisfiedWhen filter or weight×deficit sort itself.
- PR #107: Hardened the test suite for the predicate evaluator and next-actions ranker by adding a regression test that pins the internal `axisOrder()` mapping and extending the `=` equality test to assert cross-type coercion behavior. Also removed a stale git stash. No production code was modified.
- PR #108: Added three new telemetry-dated milestone detectors to the /progression timeline system in scripts/progression.mjs: 'scheduled' (fires on first /loop, /schedule, or /babysit use), 'remote' (fires on first RemoteTrigger/PushNotification/SendMessage tool invocation), and 'verification' (fires on first /go use). Also extended scripts/_usage-data.mjs's scanTranscriptModes with a per-session commands: Set<string> field, piggybacking the existing <command-name> scan loop, to feed the two transcript-dependent detectors. Live verification confirmed all three milestones fire with correctly backdated telemetry timestamps.
- PR #110: Introduces a strict posture-vs-volume partition for how the scorer counts slash-command usage from session transcripts. Posture commands (/color, /voice, /focus, /btw, /clear, /compact, /simplify, /rewind, /fewer-permission-prompts) are now only counted when the session's classifySessionKind resolves to interactive_cli or the unknown fallback, preventing observer and SDK-orchestrated sessions from double-counting a primary session's echoed <command-name> markup. Volume commands (/loop, /schedule, /babysit, /go, /batch) continue to be counted across every session kind since autonomous-workflow signal is valid regardless of session type. A fail-loud assertCommandPartition check runs at module load to catch future drift (disjointness violations, missing classification, or dead classification arms). Live verification against real transcript data showed some posture counters dropping substantially (e.g. focusCommandUses 15→1) once observer/SDK inflation was removed.
- PR #111: Migrated the nightly docs-agent GitHub Actions workflow's authentication configuration: replaced the deprecated `app-id` input with `client-id` for the GitHub App token action, and moved `JIRA_EMAIL` from repository Secrets to repository Variables since it is a non-secret basic-auth username rather than a credential.
- PR #113: Widened the self-assessment scorer's ship-journal reader so it credits `/ship` Stage 2 (verify-agent) and Stage 3 (simplify) dispatches across all three historical `~/.claude/ship/journal.jsonl` format generations (singular `entry.stage`, legacy-numeric `stages_run`, and new-string `stages_run`). A new pure helper `stageRanInEntry(entry, legacyNumber, newName)` collapses the three detection paths into one check, used by `gatherShipJournal` for both the existing `stage2Count` and a new `simplifyStageCount`. `run-assessment.mjs` MAX-merges the new journal counter into the `simplifyCommandUses` projection, and the journal lookback window widens from a hardcoded 14 days to the configurable `insightsLookbackDays` (default 30) to align with transcript-derived signals.
- PR #114: Pure file-move housekeeping: four already-shipped plan documents were relocated from docs/superpowers/plans/ into docs/superpowers/plans/archived/, with no content changes. This closes out the plans-audit report item for landed-but-unarchived plans.
- PR #116: Replaced the placeholder noTelemetry() Execution scorers for the Memory & Context Management and Terminal & Customization dimensions with real ratio scorers driven by transcript-derived posture-command coverage signals (withGates({ transcripts: true, universe: "interactive_or_unknown" })). Unified /focus and /rewind command counting from raw total-invocation counts to session-coverage counting, matching the existing pattern already used for /btw, /clear, and /compact. Added a new interactiveOrUnknownSessionsAnalyzed denominator and a new interactive_or_unknown universe option so the new ratios satisfy the numerator-subset-of-denominator invariant. With this change, all twelve rubric dimensions now have an Execution scorer.
- PR #117: Release PR bumping package.json to v0.9.18. No functional code changes in this PR itself; it packages 13 previously-merged PRs since v0.9.17, headlined by: (1) Memory + Customization Execution scorers moving from noTelemetry() to withGates ratio scorers over MAX-merged transcript+history posture-command counters, completing Execution scoring across all 12 dimensions (Model & Effort Tuning remains the only partially-measured dimension); (2) a per-command POSTURE/VOLUME partition that gates posture commands to interactive_cli ∪ unknown sessions so observer/SDK sessions can no longer inflate posture counters, backed by a fail-loud assertCommandPartition boundary check; (3) /ship Stage 2/3 dispatch counting made robust across all three ship-journal format generations; and a canonical predicate evaluator consolidation.
- PR #118: Two already-shipped plan documents (CCE-72's /ship Stage 2/3 journal-format credit and CCE-76's Memory + Customization Execution scorers) were mechanically git-moved into docs/superpowers/plans/archived/, with no content changes.
- PR #119: Fixed a data-integrity bug in the assessment scorer's signalsSummary surface: btwCommandUses no longer Math.max-blends the cumulative all-time /btw invocation count from ~/.claude.json with the 30-day windowed transcript+history session-coverage count. The fix exposes cliBtwUseCountAllTime as a separate cumulative-source field and reroutes the rubric's btw-side-channel predicate (Boris tip 33+54) to key off that cumulative field instead. A new CLAUDE.md hard rule documents the two semantic axes (time window: windowed vs cumulative; counter class: session-coverage vs raw invocation count) that must be checked before summing any field into a ratio numerator. The Memory Execution score itself is unchanged (16) since the scorer body already used maxProbe(...) directly and never consumed the corrupted summary blend.
- PR #120: Release PR bumping the project to v0.9.19, bundling two prior merges: (1) a fix to `signalsSummary.btwCommandUses` so it no longer Math.max-blends the cumulative all-time `/btw` invocation count (`~/.claude.json#btwUseCount`) into the 30-day windowed session-coverage figure — the cumulative value is now exposed separately as `cliBtwUseCountAllTime`, and the rubric's `btw-side-channel` predicate is rerouted to that field; and (2) housekeeping that archives the landed CCE-72/CCE-76 plans. Memory Execution scoring itself is unchanged (the scorer already read `maxProbe(...)` directly); this restores accuracy in the `signalsSummary` surface consumed by the predicate engine and downstream tooling.
- PR #121: Migrated the engineering-docs-agent integration from framework: none to framework: mkdocs, standing up a published Material-theme documentation site at https://theoju.github.io/claude-code-self-assessment/. Added an mkdocs.yml scaffold (search + awesome-pages + literate-nav), a pinned requirements-docs.txt, and two GitHub Actions workflows: docs-agent-pages.yml (push-to-main build+deploy via configure-pages/upload-pages-artifact/deploy-pages) and docs-build-check.yml (PR-level `mkdocs build --strict` gate, no deploy). Moved the existing flat docs/*.md files into docs/site-src/ verbatim via git mv, repaired 9 broken relative links in self-assessment.md and ship-pattern.md by rewriting cross-tree references (.claude/, docs/superpowers/specs/) to absolute GitHub blob URLs since those paths now sit outside the mkdocs docs_dir. Flipped .engineering-docs-agent/config.yml to framework: mkdocs, set whats_new_file and lens_paths.core to the new docs/site-src/ location, and added publishing.base_url / publishing.build_workflow, activating the orchestrator's publish-verifier stage.
- PR #122: Post-merge follow-up to PR #121/CCE-81 that records two operational lessons learned from the actual GitHub Pages deploy incident: (1) actions/configure-pages@v6's enablement: true does not bootstrap GitHub Pages on a first deploy because the workflow token lacks admin scope, and the real fix is a one-time `gh api -X POST .../pages -f build_type=workflow` call (or the equivalent Settings UI toggle); and (2) monitor scripts must avoid zsh's read-only `status`/`pipestatus` built-ins, which crash the shell when assigned to, masking successful deploys as false failures. The PR adds two new CLAUDE.md Conventions bullets plus a POST-IMPLEMENTATION CORRECTION section in the mkdocs-upgrade spec and a matching recovery note in the mkdocs-upgrade plan.
- PR #125: Removed the misleading `enablement: true` field from the GitHub Pages deployment workflow (`docs-agent-pages.yml`) and its accompanying comment, since it never actually bootstrapped GitHub Pages and was a no-op once Pages already existed. Flipped the matching vitest assertion into a regression guard that fails if the line is re-added. Appended a one-line "Resolved by" footer to the existing POST-IMPLEMENTATION CORRECTION block in the mkdocs upgrade design spec, and shortened the CLAUDE.md gotcha bullet to point at the plugin's CLAUDE.md as the durable source of the fix rather than re-explaining it.
- PR #127: Adds a new convention bullet to the repo's CLAUDE.md requiring that plan-step verification use the actual consumer tool (e.g. `mkdocs build --strict`, `tsc --noEmit`, `ajv validate`) rather than a filesystem existence check like `test -f`, whenever a plan step produces a published artifact (docs-site link, TS import, JSON Schema reference, API route). No code or runtime behavior changed — this is a process/documentation-only update mirrored byte-identically across three sibling repos (ADIS, engineering-docs-agent, and this repo).
- PR #128: Narrowed the Memory Execution scorer's ratio numerator from four commands (`/btw + /clear + /compact + /rewind`) down to the two semantically-consistent session-coverage signals (`/clear + /compact`). The cumulative all-time `/btw` usage is now surfaced separately as evidence text via `signalsSummary.cliBtwUseCountAllTime`, and `/rewind` is dropped from the ratio entirely but retained as a next-action probe. The rubric target for the `memory` dimension was recalibrated from 92 to 60 to match the narrowed realistic ceiling, and the methodology page narrative, probe tracker, and test fixtures were updated to match.
- PR #177: Retheme the dashboard's visual design system from a warm/gold palette to a cool-graphite instrument-panel look with a single electric-blue accent, applied globally through the Tailwind v4 `@theme` token layer in `app/globals.css`. Introduces a shared primitive layer (`.card`, `.icon-btn`, `.eyebrow`, `.pill`/`.pill-accent`, `.dot-badge`, `.tnum`), retunes semantic good/warn/bad colors to stay distinct from the new accent, and elevates `PageNav` from a breadcrumb to accent-tinted pill-tabs (preserving `aria-current` and active-detection logic). Also fixes two pre-existing broken color-token references (`--color-bg`/`--color-fg`/`--color-card` were referenced but never defined) that were causing invalid/inherited colors on the InsightsNarrative button label and ProgressionTimeline node ring.
- PR #178: Added two new Conventions entries to the project's CLAUDE.md documenting toolchain diagnosis learned during a 2026-07-30 session: (1) a Homebrew Intel→ARM migration leaving a stale `/usr/local/bin/brew` reference in `~/.zprofile`, causing login/non-interactive shells (including Claude Code's) to lose Homebrew's PATH while the interactive terminal kept working via `~/.zshrc` — diagnosed via a three-way `zsh -lc` / `zsh -ic` / `zsh -c` comparison; and (2) a follow-on `npm ci` fix for a `Cannot find module @rollup/rollup-darwin-arm64` vitest-startup failure caused by a known npm optional-dependency bug (npm/cli#4828), preferred over `rm -rf node_modules` because it needs no destructive command and doesn't rewrite the lockfile.
- PR #179: Corrected a stale test-count fact in CLAUDE.md (564 tests/39 files/~5s → 695 tests/48 files/~3s), added two new Conventions entries documenting recurring ambiguities encountered during a 2026-07-30 session — a 'how many Boris tips?' disambiguation (87 upstream vs 86 captured vs 75 tracked-and-scored) and two known `/graphify` extraction artifacts specific to this repo's data shape (tip-index fragmentation inflating community counts, and dangling cross-chunk edges being silently dropped at build time) — and added a companion session retrospective document capturing the narrative context that doesn't belong in a terse rule.
- PR #186: Archived two already-landed planning documents (the mkdocs upgrade plan from PR #122 and the CCE-79 memory scorer redesign plan from PR #128) by moving them into docs/superpowers/plans/archived/. Both PRs they described had already merged; only file locations moved (100% rename similarity), with no content changes.
### Gaps flagged
- theoju/claude-code-self-assessment#100: Middle tier (empty allowlist, size_filter has no min_loc/min_files so the below-threshold check can't be satisfied). LLM judgment: this PR bootstraps a new subsystem — onboarding the engineering-docs-agent plugin with host config, a state seed, and a new recurring nightly GitHub Actions workflow that automates doc generation/publishing. Introducing a new automated pipeline with persistent state and CI wiring is exactly the kind of system-level, ongoing-behavior change a senior engineer would expect a spec or plan to precede, even though the file count is small.
- theoju/claude-code-self-assessment#106: Falls in the 'middle' tier (no allowlist match, size_filter thresholds unspecified/empty). Substantively, this PR introduces a new canonical DSL evaluator subsystem (scripts/predicate.mjs) and a new pre-computed ranking pipeline (scripts/rank-next-actions.mjs) wired into run-assessment.mjs, adding a new field (rankedNextActions) to the assessment.json output contract that downstream consumers (the self-assessment skill) rely on. It's explicitly framed as 'PR 2 of 2' fixing a recurring architectural bug class, and touches CLAUDE.md governance rules plus a passthrough-invariant test — the kind of cross-cutting behavioral/contract change a senior engineer would expect a spec or plan to precede, even though tests are included.
- theoju/claude-code-self-assessment#108: No allowlist paths configured and size_filter thresholds are unset, so this falls to LLM judgment. The PR adds three new telemetry-dated milestone detectors (scheduled/remote/verification) to the progression subsystem, a new-feature change that extends scored coverage and introduces new detection logic consuming transcript data. This is the kind of subsystem-level feature work a senior engineer would expect a spec/plan for — and indeed the PR itself includes both a design spec (docs/superpowers/specs/2026-06-01-cce-33-progression-detectors-design.md) and an archived plan (docs/superpowers/plans/2026-05-31-cce-33-progression-detectors.md), consistent with this repo's convention of pairing feature work with spec+plan docs.
- theoju/claude-code-self-assessment#110: Allowlist is empty (no match) and size_filter thresholds are unset in config, so no allowlist/size_filter tier applies. Falling to LLM judgment: this PR introduces a new posture-vs-volume command partition semantics with a fail-loud invariant check (assertCommandPartition) added to core scoring logic (scripts/_usage-data.mjs), changing how telemetry counters are computed and correcting a class of false positives. This is exactly the kind of behavior-changing, subsystem-level change a senior engineer would expect a written spec/plan for — and indeed the PR body cites both a validated spec and plan doc, confirming the expectation was met in practice.
- theoju/claude-code-self-assessment#113: No allowlist paths configured and size_filter thresholds are unset, so falls to LLM judgment. This PR changes scoring behavior (widens which /ship journal entries count toward simplifyCommandUses, adds a new stageRanInEntry helper, widens lookback window from 14 to 30 days) — a user-visible change to how Execution scores are computed. A senior engineer would expect a spec/plan for this, and indeed the PR itself references and includes a design spec and implementation plan (docs/superpowers/specs/2026-06-01-ship-journal-stage-credit-design.md and docs/superpowers/plans/2026-06-01-ship-journal-stage-credit.md), consistent with expectations for scoring-logic changes.
- theoju/claude-code-self-assessment#116: No allowlist match (allowlist_paths empty) and size_filter has no thresholds set, so falls to LLM judgment tier. The PR replaces noTelemetry() scorers with new withGates() ratio scorers for two dimensions, introduces a new 'interactive_or_unknown' universe and a new denominator field, and changes counting semantics for two commands from total-invocation to session-coverage — exactly the class of ratio/denominator-semantics change this repo's own hard rules (numerator-subset-of-denominator, per-field time-window/counter-class categorization) flag as needing a design doc before implementation. A senior engineer would expect a spec/plan here, and in fact the PR itself cites one (validated by three independent agents), corroborating that this change class is spec-worthy.
- theoju/claude-code-self-assessment#121: Middle tier (allowlist empty, size_filter config empty so no threshold to fall below; 2940 LOC / 26 files is substantial regardless). This introduces a new user-facing subsystem — a published Material-theme docs site with a new domain (theoju.github.io), two new CI workflows (deploy + PR-gate), a config flip in .engineering-docs-agent/config.yml activating a new pipeline stage, and a file-migration with link-rewriting risk. A senior engineer would expect a spec/plan for this scope, and indeed the PR itself supplies both (docs/superpowers/specs/2026-06-01-mkdocs-upgrade-design.md and docs/superpowers/plans/2026-06-01-mkdocs-upgrade.md), consistent with that expectation.
- theoju/claude-code-self-assessment#128: No allowlist patterns configured and size_filter thresholds are unset, so falls to LLM judgment. This PR changes the Memory Execution scoring algorithm (narrows ratio numerator, recalibrates rubric target 92→60, changes how /btw and /rewind signals surface) — a user-visible behavior change to the scoring engine that affects reported scores. This is exactly the class of change (new/changed scoring logic affecting output) a senior engineer would expect a written spec/plan for, and indeed the PR itself references a spec and plan doc it authored.

## 2026-08-13 — Docs site is now published (mkdocs + GitHub Pages)

This page — and the rest of `docs/site-src/` — is now built into a
real, searchable site at
<https://theoju.github.io/claude-code-self-assessment/>, instead of
sitting as flat unpublished markdown. PR #121 flipped the
engineering-docs-agent integration from `framework: none` to
`framework: mkdocs` in `.engineering-docs-agent/config.yml`, which
activated the orchestrator's publish-verifier stage.

What changed under the hood:

- **New `mkdocs.yml` scaffold** at the repo root: Material theme,
  `search` + `awesome-pages` + `literate-nav` plugins (nav is driven
  by `docs/site-src/SUMMARY.md`), pinned dependencies in
  `requirements-docs.txt`.
- **Two GitHub Actions workflows**: `docs-agent-pages.yml` builds and
  deploys the site on every push to `main` that touches
  `docs/site-src/**`, `mkdocs.yml`, or `requirements-docs.txt`
  (`mkdocs build --strict` → `actions/upload-pages-artifact` →
  `actions/deploy-pages`); `docs-build-check.yml` runs the same
  `mkdocs build --strict` gate on PRs touching those paths, without
  deploying — so a broken link is a review-time failure, not a
  post-merge surprise.
- **The existing flat `docs/*.md` files moved into `docs/site-src/`**
  verbatim via `git mv`. Nine relative links inside
  `self-assessment.md` and `ship-pattern.md` that pointed outside the
  new `docs_dir` (into `.claude/` or `docs/superpowers/specs/`) were
  rewritten to absolute GitHub blob URLs, since mkdocs's strict mode
  rejects link targets outside `docs_dir`.

If you're contributing docs going forward: everything under
`docs/site-src/` is agent-editable and gets built by the workflow
above — verify locally with `mkdocs build --strict` before opening a
PR, the same check `docs-build-check.yml` runs in CI.

See the design record for the full rationale:
[2026-06-01 — mkdocs upgrade](2026-06-02-mkdocs-upgrade.md).
