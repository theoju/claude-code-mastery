# What's New

The engineering-docs-agent appends entries here on each nightly run
when it detects merged work worth surfacing in a user-facing
changelog. Most entries are written by the agent and reviewed via
the `docs-agent/YYYY-MM-DD` PR.

<!-- The first authored entry will land below on the first nightly
     after the mkdocs upgrade ships. -->
## 2026-06-23T08:20:56.009695+00:00
- PR #101: Adds a Conventions entry to CLAUDE.md documenting the /progression timeline's dual milestone-source architecture: telemetry detectors (scripts/progression.mjs) self-date from session start_time across full history, while config detectors (scripts/config-progression.mjs) stamp firstSeenAt at first-observed and freeze it in progression-config.json. Records the deliberate first-run caveat explaining why all 8 config milestones share the identical 2026-05-09 timestamp, and documents the coverage gap where 3 of 12 scored dimensions (scheduled, remote, verification) have no milestone detector — tracked as CCE-33. Also fixes two file-map entries in CLAUDE.md: adds the missing config-progression.mjs reference and corrects the stale comment on app/progression/page.tsx.
- PR #100: PR #100 onboards the claude-code-self-assessment repository onto the engineering-docs-agent plugin. It adds the host configuration file (`.engineering-docs-agent/config.yml`) using `framework: none` (introduced by CCE-64), seeded state files, and a nightly GitHub Actions workflow that runs the full agent pipeline. An earlier synthetic mkdocs scaffold was removed because `framework: none` makes it unnecessary. The nightly workflow wires in all recent plugin fixes: App-token wiring (CCE-45), OAuth validation (CCE-49), forensics upload (CCE-41), and Jira wiring (CCE-53).
- PR #102: Fixed a one-character JSON schema bug in `.engineering-docs-agent/state.json` where `dismissed_gap_flags` was bootstrapped as an empty array (`[]`) instead of an empty object (`{}`). The plugin's state schema requires this field to be an object keyed by `{owner}/{name}#{pr}`, and strict schema validation caused the orchestrator to exit with code 2 within ~0.2 s of startup, blocking every docs-agent nightly run entirely.
- PR #104: Added a DSL grammar reference block (7 operator classes, canonical-implementation pointer, worked example) to the /self-assessment skill's SKILL.md to prevent the recurring bug where a model re-implements the `satisfiedWhen` evaluator incorrectly at runtime. Also ships the design spec and implementation plan for the structural predicate-ranker fix (pre-computing `rankedNextActions` into `assessment.json`) targeted in the follow-up PR 2. The grammar block in SKILL.md is explicitly temporary and will be removed once `scripts/predicate.mjs` + `assessment.json.rankedNextActions` land.
- PR #106: Extracted a canonical DSL predicate evaluator (`scripts/predicate.mjs`) as the single source of truth for `satisfiedWhen` evaluation, eliminating the risk of in-model re-implementations. The `app/lib/assessment.ts:evaluatePredicate` function is now a one-line re-export passthrough (enforced by a reference-identity equivalence test). A new `scripts/rank-next-actions.mjs` module provides `rankNextActions()`, which filters out already-satisfied next-actions via the canonical evaluator and ranks the remainder by weight × deficit with a deterministic five-tier tie-break. This is wired into `scripts/run-assessment.mjs` so every `npm run assess` writes a pre-computed `rankedNextActions[10]` array into `assessment.json`. The `.claude/skills/self-assessment/SKILL.md` was simplified to a single instruction to read from that pre-computed array. Two hard rules were added to CLAUDE.md pinning the single-evaluator and pre-computed-ranking contracts. Thirty new tests cover DSL operator evaluation, the named regression (`loopCommandUses=14` correctly excludes the already-satisfied `babysit-loop` action), and the TypeScript passthrough equivalence. The `Assessment` TypeScript interface was updated to expose `RankedNextAction[]`.
- PR #107: Two test-hardening additions from post-PR-#106 review-debt cleanup. First, a new explicit regression test pins the internal `axisOrder()` sort mapping (platform=0 / execution=1 / else=2) so any future refactor that reorders the enum fails loudly rather than silently re-ranking the dashboard. Second, the existing `=` equality predicate test is extended to assert `evaluatePredicate("x=5", { x: "5" })` returns `true`, locking the `String(value) === lit` cross-type coercion contract against a future strict-equality refactor. A superseded stash WIP entry (`stash@{0}`) is also dropped. No production code was touched.
- PR #108: Closes the `/progression` timeline coverage gap (CCE-33) by adding three new telemetry-dated milestone detectors to `scripts/progression.mjs`: `scheduled` (Boris tip 48, fires on first `/loop`/`/schedule`/`/babysit` invocation), `remote` (tip 35, fires on first `RemoteTrigger`/`PushNotification`/`SendMessage` tool use), and `verification` (tip 73, fires on first `/go`). Previously only 8 of 12 scored dimensions had progression detectors; this lands the missing three. A supporting change extends `scanTranscriptModes` in `scripts/_usage-data.mjs` with a per-session `commands: Set<string>` field consumed by the two transcript-dependent detectors. The probe-tracker spec is synced in the same PR per CLAUDE.md hard rules; the five machine-enforced header counts are unchanged (75/12/48/47/71). Live verification confirms all three milestones fire with backdated telemetry timestamps.
- PR #110: Introduced a formal posture-vs-volume command partition in the transcript scanner (`scripts/_usage-data.mjs`). Posture commands (`/color`, `/voice`, `/focus`, `/btw`, `/clear`, `/compact`, `/simplify`, `/rewind`, `/fewer-permission-prompts`) are now counted only when `classifySessionKind` returns `interactive_cli` or `unknown`, preventing observer and SDK-orchestrated sessions from inflating user-posture counters by echoing the primary session's command markup. Volume commands (`/loop`, `/schedule`, `/babysit`, `/go`, `/batch`) continue to be counted across all session kinds. A fail-loud `assertCommandPartition` module-load assertion enforces disjointness and completeness at startup. Eleven new tests cover the helper and fixture paths, and a design spec plus implementation plan were committed to the project docs.
- PR #111: Migrated the docs-agent-nightly GitHub Actions workflow from the deprecated `app-id` input to `client-id` in `actions/create-github-app-token@v3`, and moved `JIRA_EMAIL` from repository secrets to repository variables (`vars.JIRA_EMAIL`), reflecting that it is a non-sensitive username rather than a credential.
- PR #113: PR #113 introduces a new pure helper `stageRanInEntry(entry, legacyNumber, newName)` in `scripts/signals.mjs` that detects `/ship` stage execution across all three journal format generations (singular `entry.stage`, legacy-numeric `stages_run` integer array, new-string `stages_run` string array). Previously `gatherShipJournal` only read `entry.stage === 2`, silently missing ~41% of journal entries and causing `simplifyCommandUses=0` for users with `/ship` deeply integrated. The fix widens `stage2Count` detection and introduces a new `simplifyStageCount` counter; `run-assessment.mjs` MAX-merges `simplifyStageCount` into the `simplifyCommandUses` projection (mirroring the v0.9.16 `/color` history pattern). The journal-signal lookback is also widened from 14 → `insightsLookbackDays` (default 30) to align with transcript-derived signals. 17 new or widened unit tests cover all three format generations, and the probe-tracker spec is annotated with a `[^journal-stage-credit]` footnote on the three affected rows.
- PR #114: Four previously-shipped plan documents were moved from `docs/superpowers/plans/` into `docs/superpowers/plans/archived/`. The plans cover runtime-adoption-probes (PR #94), CCE-33 progression detectors (PR #108), per-command partition (PR #110), and predicate ranker (PR #104). No content was altered; the change is a pure file rename/move to keep the active plans folder uncluttered.
- PR #116: PR #116 (CCE-76) activates Execution scorers for two previously unmeasured dimensions — Memory & Context Management and Terminal & Customization — making all twelve scoring dimensions fully measured. It replaces the placeholder `noTelemetry()` returns with `withGates({ transcripts: true, universe: "interactive_or_unknown" })` ratio scorers that consume transcript-derived posture-command session-coverage counters. As part of this, `focusCommandUses` and `rewindCommandUses` are unified from per-message to per-session counting (matching the canonical pattern already used by `/btw`, `/clear`, `/compact`), and a new `interactiveOrUnknownSessionsAnalyzed` denominator signal is added to close the numerator-must-be-a-subset-of-denominator hard rule established in PR #97. The Execution composite drops from 77 → 66 because two low-scoring dimensions (Memory 16, Customization 3) join the average — this is honest measurement, not regression. 19 new tests, full suite passes at 666.
- PR #117: Release v0.9.18 bumps package.json and bundles 13 PRs landed since v0.9.17 (2026-05-26). Key user-facing changes: (1) Memory and Customization Execution scorers replace noTelemetry() with transcript-backed ratio scorers gated to the interactive_or_unknown session universe, completing Execution coverage across all 12 scoring dimensions; (2) a per-command POSTURE/VOLUME partition prevents observer/SDK sessions from inflating posture-command counters, enforced by a fail-loud assertCommandPartition check at module load; (3) /ship Stage 2 (verify-agent) and Stage 3 (simplify) dispatch counts now work across all three journal format generations, fixing an adoption-signal undercount; (4) scripts/predicate.mjs becomes the single canonical DSL evaluator, with app/lib/assessment.ts reduced to a passthrough re-export and pre-computed rankedNextActions landing in assessment.json; (5) the /progression timeline gains telemetry-dated detectors for scheduled, remote, and verification dimensions; and several supporting fixes including the dismissed_gap_flags shape correction and Jira workflow migration.
- PR #118: Archived two shipped plan documents (CCE-72 and CCE-76) by moving them from the active plans directory into `docs/superpowers/plans/archived/` via a mechanical `git mv`. No content was altered; the files simply reflect that the corresponding work (ship-journal stage-credit and Memory/Customization Execution scorers) has landed in v0.9.18.
- PR #119: Removed the Math.max blend that conflated two semantic axes in signalsSummary.btwCommandUses: the cumulative all-time ~/.claude.json btwUseCount was being mixed into the 30-day windowed session-coverage counter. The fix splits the field: btwCommandUses now reflects only 30-day windowed transcript+history session-coverage, while a new cliBtwUseCountAllTime field exposes the cumulative source separately. The btw-side-channel rubric predicate (Boris tips 33/54) was rerouted from btwCommandUses>=1 to cliBtwUseCountAllTime>=1, which better matches the 'have you ever adopted this habit' semantic. Three new tests enforce the field split, probe-tracker counts updated (probes 47→48, signalsSummary 71→72), and a CLAUDE.md hard rule codifies the cumulative-vs-windowed/counter-class distinction to prevent recurrence.
- PR #120: Cuts v0.9.19, a scoring-honesty patch bundling two previously-landed PRs. The primary fix (CCE-78, PR #119) removes the Math.max blend at scripts/run-assessment.mjs:134 that incorrectly merged a cumulative all-time /btw invocation count (from ~/.claude.json#btwUseCount) into the 30-day windowed signalsSummary.btwCommandUses session-coverage field. The cumulative value is now exposed separately as cliBtwUseCountAllTime, and the rubric btw-side-channel predicate (tips 33 and 54) is rerouted to target that field. Also archives the CCE-72 and CCE-76 landed plans (PR #118), and adds a CLAUDE.md hard rule codifying cumulative-vs-windowed counter semantics. Memory Execution score is unchanged by design; signalsSummary.btwCommandUses now accurately reflects only 30-day windowed session coverage.
- PR #121: Upgraded the engineering-docs-agent integration from `framework: none` to `framework: mkdocs`, publishing a Material-theme docs site at https://theoju.github.io/claude-code-self-assessment/. The PR adds `mkdocs.yml` (with search, awesome-pages, and literate-nav plugins), a pinned `requirements-docs.txt`, and two GitHub Actions CI workflows: a push-to-main deploy workflow (`docs-agent-pages.yml`) and a PR-level `mkdocs build --strict` gate (`docs-build-check.yml`). Four existing `docs/*.md` files and their images were migrated into `docs/site-src/`; broken cross-tree relative links were rewritten to absolute GitHub blob URLs. The engineering-docs-agent config was flipped to `framework: mkdocs` with `base_url` and `build_workflow` filled in, activating the nightly publish-verifier stage. Six other files were retargeted to the new paths. Three new Vitest test files (21 cases) cover path migration, scaffold existence, and config contract.
- PR #122: Post-merge follow-up to PR #121 / CCE-81 recording three durable lessons from the actual GitHub Pages deploy incident. (1) `actions/configure-pages@v6 enablement: true` does NOT bootstrap Pages on a first deploy — the workflow's GITHUB_TOKEN lacks admin scope; fix is a one-time `gh api -X POST repos/.../pages -f build_type=workflow` call or the Pages UI, and the line should be deleted from workflows as a misleading no-op. (2) Monitor scripts must avoid assigning to zsh's reserved built-in names (`status`, `pipestatus`), which crash the shell; fix is to rename loop locals or shebang bash. (3) All three rollout deviations are now recorded in the mkdocs upgrade spec (post-implementation correction block under Gate 5) and plan (post-merge outcomes section). CLAUDE.md gains two new Conventions bullets capturing both lessons for future hosts onboarded with `framework: mkdocs`.
- PR #125: Dropped the misleading `enablement: true` line from `.github/workflows/docs-agent-pages.yml` (the field is a no-op once GitHub Pages is bootstrapped because the workflow token lacks the admin scope needed to call the Pages API), flipped the corresponding vitest assertion from `toMatch` to `not.toMatch` as a regression guard, appended a one-line 'Resolved by' footer to the POST-IMPLEMENTATION CORRECTION block in the mkdocs upgrade spec, and shortened the CLAUDE.md Pages-bootstrap gotcha bullet to reference the engineering-docs-agent plugin's CLAUDE.md as the durable explanation. No runtime or behavioral change.
- PR #127: Added a new CLAUDE.md convention rule requiring plan-step verification to invoke the actual consumer tool (e.g., `mkdocs build --strict`, `npx tsc --noEmit`) rather than relying on filesystem presence checks (`test -f`). The rule text was propagated byte-identically (MD5 `c211304951e64e7a3bea48fbf923ab28`) across three repositories — this repo, ADIS, and EDA — as part of a coordinated cross-repo convention rollout.

